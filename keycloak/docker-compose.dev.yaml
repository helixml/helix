# Development Keycloak for local testing
#
# Usage:
#   # Create network if Helix isn't running yet
#   docker network create helix_default 2>/dev/null || true
#
#   # Start keycloak
#   docker compose -f keycloak/docker-compose.dev.yaml up -d
#
# Keycloak Admin Console: http://localhost:8180/auth/admin
# Username: admin
# Password: admin (or KEYCLOAK_ADMIN_PASSWORD from .env)

services:
  keycloak:
    build:
      context: ..
      dockerfile: keycloak/Dockerfile
    image: helix-keycloak:local
    restart: unless-stopped
    ports:
      - "8180:8080"
    environment:
      - KC_DB=postgres
      - KC_DB_URL=jdbc:postgresql://keycloak-postgres:5432/keycloak
      - KC_DB_USERNAME=postgres
      - KC_DB_PASSWORD=${KEYCLOAK_POSTGRES_PASSWORD:-keycloak}
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD:-admin}
      - KC_PROXY_HEADERS=forwarded|xforwarded
      - KC_HEALTH_ENABLED=true
      - KC_HOSTNAME_PATH=/auth
      - KC_HTTP_RELATIVE_PATH=/auth/
      - KC_HOSTNAME_URL=${KEYCLOAK_FRONTEND_URL:-http://localhost:8180/auth}/
      - KC_HOSTNAME_ADMIN_URL=${KEYCLOAK_FRONTEND_URL:-http://localhost:8180/auth}/
      - KC_HOSTNAME_STRICT=true
      # Dev mode: disable theme caching for live editing
      - KC_SPI_THEME_CACHE_THEMES=false
      - KC_SPI_THEME_CACHE_TEMPLATES=false
      - KC_SPI_THEME_STATIC_MAX_AGE=-1
    volumes:
      # Mount themes directory for development
      - ../themes:/opt/keycloak/themes
    command: ["start-dev"]
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080;echo -e 'GET /auth/health/ready HTTP/1.1\\r\\nhost: http://localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3;grep 'HTTP/1.1 200 OK' <&3"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    depends_on:
      keycloak-postgres:
        condition: service_healthy
    networks:
      - keycloak-net
      - helix_default

  keycloak-postgres:
    image: postgres:12.13-alpine
    restart: unless-stopped
    volumes:
      - keycloak-postgres-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=keycloak
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${KEYCLOAK_POSTGRES_PASSWORD:-keycloak}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d keycloak"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - keycloak-net

volumes:
  keycloak-postgres-data:

networks:
  keycloak-net:
    name: keycloak_network
  helix_default:
    external: true
