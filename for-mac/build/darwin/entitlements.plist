<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for QEMU HVF (Hypervisor.framework) acceleration -->
    <key>com.apple.security.hypervisor</key>
    <true/>
    <!-- Required for QEMU to use vmnet.framework for networking -->
    <key>com.apple.vm.networking</key>
    <true/>
    <!-- Allow JIT compilation (QEMU TCG fallback) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (QEMU code generation) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow DYLD environment variables (for loading bundled frameworks) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation (load bundled open-source frameworks) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
