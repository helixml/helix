linters:
  enable:
    - staticcheck
    - unconvert
    - gofmt
    - goimports
    - ineffassign
    - gosimple
    - govet
    - misspell
    - unused
    - bodyclose
    - tparallel
    - errcheck
    #- revive

issues:
  exlude-dirs:
    - vendor

run:
  timeout: 10m
