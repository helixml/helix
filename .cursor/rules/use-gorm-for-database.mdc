---
description: Database interactions should be using Gorm
globs: api/pkg/store/*.go
alwaysApply: false
---
**Database Access:**
- Use Gorm: `s.gdb.WithContext(ctx).Where(xx).First(xx)`
- Complex queries with joins: Use Raw: `s.gdb.WithContext(ctx).Raw("query").Scan(&result)`
