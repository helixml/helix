diff --git a/api/pkg/server/server.go b/api/pkg/server/server.go
index abc123..def456 100644
--- a/api/pkg/server/server.go
+++ b/api/pkg/server/server.go
@@ -850,6 +850,9 @@ func (apiServer *HelixAPIServer) registerRoutes(_ context.Context) (*mux.Router
 	// Kodit routes
 	authRouter.HandleFunc("/kodit", system.DefaultWrapper(apiServer.createKodit)).Methods(http.MethodPost)

+	// Security telemetry monitoring routes
+	apiServer.registerSecurityRoutes(subRouter)
+
 	router.HandleFunc("/api/v1/ws", apiServer.websocketUpgrade)

 	// RevDial for external agents (Zed, VS Code, etc)

---

# Apply this patch with:
# cd ~/pm/helix
# patch -p1 < SECURITY_API_INTEGRATION.patch

# Or manually add the following line to api/pkg/server/server.go
# around line 850-860 in the registerRoutes() function:

apiServer.registerSecurityRoutes(subRouter)
