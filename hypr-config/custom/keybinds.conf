# Additional keybinds for development workflow

# Basic development shortcuts
bind = SUPER, Return, exec, ghostty || foot || kitty
bind = SUPER, T, exec, ghostty || foot || kitty

# Application launcher (use fuzzel from illogical-impulse)
bind = SUPER, D, exec, fuzzel

# Quick development tools
bind = SUPER SHIFT, Z, exec, zed
bind = SUPER SHIFT, C, exec, code