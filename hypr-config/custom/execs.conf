# Container-specific startup overrides for illogical-impulse

# Start a terminal immediately to fix black screen issue
exec-once = bash -c 'sleep 3 && (ghostty || kitty || foot)'

# Start another terminal with shorter delay as backup
exec-once = bash -c 'sleep 1 && (ghostty || kitty || foot)'