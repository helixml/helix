# Startup applications and scripts

# Install tools on first run
exec-once = /install-ghostty.sh

# Start a terminal for testing (delay to let everything load)
exec-once = bash -c 'sleep 8 && (ghostty || foot || kitty)'

# Start desktop components if available - DISABLED: Started by startup script instead
# exec-once = bash -c 'sleep 5 && which ags >/dev/null 2>&1 && ags || echo "AGS not available"'