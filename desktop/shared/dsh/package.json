{
  "name": "helix-dsh-acp",
  "version": "0.0.0",
  "private": true,
  "description": "DeepSeek Harness for Helix sandboxes, run as `dsh --profile acp`. Pinned by package-lock.json; regenerate with `npm install` after editing the version here.",
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.7-rc.1"
  }
}
