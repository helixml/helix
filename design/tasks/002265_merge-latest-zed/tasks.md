# Implementation Tasks: Merge Latest Zed Upstream Into Helix Fork

## Setup & Read (mandatory before any merge resolution)

- [ ] Read prior spec `002251_merge-latest-zed/` end-to-end (closest precedent — ACP `0.14→1.1`)
- [ ] Read prior spec `002224_merge-latest-zed/` end-to-end (brief-referenced; ACP `0.14→1.0.1`)
- [ ] Skim `002077_`, `002100_`, `002029_merge-latest-zed/` for conflict/critical-fix patterns
- [ ] `cd /home/retro/work/zed`; `git fetch origin`; add `upstream` remote if missing; `git fetch upstream --no-tags`
- [ ] `git checkout main && git pull origin main`
- [ ] Read `/home/retro/work/zed/portingguide.md` in full (Critical Fixes, Rebase Checklist, Commit History, latest Merge entry)

## Measure the Window (record actual values)

- [ ] `git merge-base main upstream/main` — record the fence
- [ ] `git log --oneline main..upstream/main | wc -l` — record commit count
- [ ] `git log --oneline -1 upstream/main` — record upstream HEAD SHA + date
- [ ] `grep -A2 'name = "agent-client-protocol"' Cargo.lock` — record ACP version; note if bumped
- [ ] Enumerate Helix-only commits on fork main since the fence — confirm they will ride the merge
- [ ] Per-hot-spot churn: `git log --oneline main..upstream/main -- <path> | wc -l` for each HIGH file
- [ ] Set risk profile from measured size + ACP state; escalate early if the window looks unusually large

## Merge Execution

- [ ] Create branch `feature/002265-merge-latest-zed` from fork main
- [ ] Start the `## Merge 002265 (2026-07-20)` section in `portingguide.md` NOW (before resolving)
- [ ] `git merge upstream/main`
- [ ] `Cargo.lock` → `git checkout --theirs` (regenerated by build)
- [ ] `.github/workflows/` → accept upstream; confirm `.drone.yml` `--locked` untouched
- [ ] Take upstream's `agent-client-protocol` pin in `Cargo.toml`; keep Helix members + `rust-embed debug-embed`
- [ ] Resolve each source conflict preserving Helix behaviour; write a porting-guide bullet as EACH is resolved
- [ ] Reconcile `settings_content/` + `agent_settings/` new fields (both-sides-added-field pattern)
- [ ] Reconcile `grep_tool.rs` 001410 `truncate_long_lines` with upstream churn
- [ ] Confirm no cfg-gated Helix dependency on any upstream-deleted `agent_configuration/*_modal.rs`
- [ ] No conflict markers remain; commit the merge

## ACP Bump (only if version changed this window)

- [ ] Build-drive every broken call site; fix compile errors area by area (`agent_servers/src/acp.rs`, `acp_thread/`, `agent/src/`, `external_websocket_sync/`)
- [ ] Convert `non_exhaustive` struct literals to builders; re-check `ErrorCode` variants
- [ ] Update exhaustive matches for any new/renamed ACP event / stop-reason / thread-status variant
- [ ] Re-verify (or re-implement) Fix #6/#9 `stopped_emitted_for_task` against any status-event refactor
- [ ] Document every repaired site in `### ACP bump — repaired sites`
- [ ] Escalate to the user if any ACP change is not a mechanical fix

## Verify Critical Fixes (grep + build)

- [ ] Fix #1 `load_session`/`pending_sessions` — `agent/src/agent.rs`
- [ ] Fix #2 no duplicate WS sends — `agent_ui/src/acp/thread_view.rs`
- [ ] Fix #3 `content_only()` — `acp_thread.rs`
- [ ] Fix #4 `notify_thread_display()` — `thread_service.rs`
- [ ] Fix #5 flush-stale-pending — `thread_service.rs`
- [ ] Fix #6/#9 `stopped_emitted_for_task` guards — `acp_thread.rs`
- [ ] Fix #7 `THREAD_REGISTRY` unregistration — `thread_view.rs`
- [ ] Fix #8 `drop(send_task)` — `acp_thread.rs`
- [ ] Fix #11 entity-identity guard (`ThreadMetadataStore`) — `agent_panel.rs`

## Verify Helix Surface (brief constraints 1–13)

- [ ] `crates/external_websocket_sync/` intact incl. recent PR surface (crash-frame, Codex-ACP, unified send)
- [ ] `ThreadDisplayNotification` still calls `OnboardingUpsell::set_dismissed(true, cx)` + initialises `NativeAgentSessionList`
- [ ] Fix 1b draft-suppression `return;` re-located as FIRST statement of its `BaseView::Uninitialized` branch
- [ ] `send_agent_ready` / `wait_for_websocket_connected` / UI-state-query callback / `acp_history_store()` in `agent_panel.rs`
- [ ] `from_existing_thread()` field set matches its `::new` sibling (signature-drift site)
- [ ] `AcpBetaFeatureFlag::enabled_for_all() -> true`
- [ ] Built-in agent hiding wrapped in `cfg(not(feature = "external_websocket_sync"))`
- [ ] Windowless `cx.subscribe()` in `thread_service.rs` preserved (incremental streaming)
- [ ] `--allow-multiple-instances` + `--headless` + `build_application(headless)` — `main.rs`
- [ ] `trust_all_worktrees: true`, `show_sign_in: false`, branding — `assets/settings/default.json`
- [ ] `title_bar` `optional = true` + `render_restricted_mode`; `rust-embed debug-embed`; `wait_for_tools_ready` uses `background_executor().timer()` (no `smol::Timer`)
- [ ] 3× `// HELIX: External agent` markers in `extensions_ui.rs`
- [ ] `BaseView` / `ContextServerStatus` (and any new ACP status) matches stay exhaustive

## Build & Test (hard gates)

- [ ] `cd /home/retro/work/helix && ./stack build-zed dev` — zero errors
- [ ] `cargo check` passes with AND without `--features external_websocket_sync`
- [ ] `.drone.yml` builds with `--locked`
- [ ] (if local Rust) `cargo test -p external_websocket_sync`
- [ ] (if local Rust) `cargo test -p acp_thread test_second_send`
- [ ] (if local Rust) `cargo test -p agent_servers test_concurrent_session_creation_is_serialized`
- [ ] `go mod tidy` in `e2e-test/helix-ws-test-server/`; copy fresh binary to `e2e-test/zed-binary`
- [ ] Enumerate the current E2E phase list at runtime (do not assume a count)
- [ ] E2E `zed-agent` — all phases green (incl. brief Phase 1–4 + UI-state queries)
- [ ] E2E `zed-agent,claude` — all phases green for both agents
- [ ] One retry permitted per agent for the Claude Phase-1 npm / single API-latency flake

## Update `portingguide.md` (incremental — not at the end)

- [ ] `## Merge 002265 (2026-07-20)` section at top of merge-history list
- [ ] Window summary (measured commits / window / upstream HEAD / fence / ACP state)
- [ ] `### Conflicts and Resolutions` per manual conflict (or explicit "0 conflicts")
- [ ] `### ACP bump — repaired sites` (or "no ACP bump this window")
- [ ] `### Retired Helix patches` with justification (or explicit "none")
- [ ] Helix-surface survival-check subsection
- [ ] Commit-history table extended; stale entries corrected/deleted; new Rebase-Checklist entry only if genuinely new

## Finalise

- [ ] Re-fetch `upstream/main` + `origin/main`; absorb out-of-band fork-main commits; target newer HEAD or run a `002265-extension` round if upstream advanced materially
- [ ] Push Zed branch `feature/002265-merge-latest-zed` to `origin` (mirrors `helixml/zed`)
- [ ] Bump `sandbox-versions.txt` `ZED_COMMIT` to the new merge HEAD on `feature/002265-merge-latest-zed` in the helix repo; push
- [ ] Write `pull_request_zed.md` + `pull_request_helix.md` into this task directory
- [ ] No force-push to `main` without explicit user approval; no agent-initiated PRs (Helix UI handles creation)
