# Requirements: Merge Latest Zed Upstream Into Helix Fork

## Context

Today is **2026-07-20**. This is the next cycle in the recurring
`zed-industries/zed` → Helix fork upstream-merge series. It follows a long line
of completed merges; the two closest precedents are:

- **002251** (planned 2026-07-13) — the largest window in the series: 409 commits
  over ~25 days, carrying the **ACP `0.14.0 → 1.1.0`** major protocol bump. Targeted
  upstream HEAD `aeeacf5439` (2026-07-12).
- **002224** (2026-07-06, superseded — spec `spt_01kwtse2gwbmgrjkmekvm45zax`) — the
  spec the task brief asks us to read first. 289 commits, ACP `0.14.0 → 1.0.1`.

Both specs are captured in `helix-specs/design/tasks/002224_merge-latest-zed/`
and `.../002251_merge-latest-zed/`. Their design/requirements/tasks documents are
the authoritative playbook for this merge; this spec extends them for the
2026-07-20 window rather than re-deriving them.

### Baseline is NOT yet measured

The working clone (`/home/retro/work/zed`) is a fresh empty clone at planning time
(no HEAD, no `upstream` remote, `portingguide.md` not checked out). **The
implementation agent must measure the actual divergence at runtime** — do not
trust any commit count in this document as final. Expected picture:

- If 002251 landed on fork main, this window is roughly **2026-07-12 → 2026-07-20
  (~8 days)** — likely a smaller catch-up (order ~100–150 upstream commits), with
  the ACP crate probably already at `1.1.0` (check for a further bump).
- If 002251 did **not** land, the window is larger and may still carry the
  `0.14 → 1.x` ACP bump. Measure and adjust risk accordingly.

Runtime measurement (mandatory first step):

```bash
cd /home/retro/work/zed
git fetch origin
git remote add upstream https://github.com/zed-industries/zed.git   # if missing
git fetch upstream --no-tags
git checkout main && git pull origin main
git merge-base main upstream/main                     # fence
git log --oneline main..upstream/main | wc -l         # commits to merge
git log --oneline -1 upstream/main                    # upstream HEAD to record
grep -A2 'name = "agent-client-protocol"' Cargo.lock  # ACP version — check for bump
```

## Repository Layout (sandbox reality — verify before pushing)

- **Working repo**: `/home/retro/work/zed/` — `origin` = in-cluster gitea mirror
  `http://api:8080/git/prj_01kg02vqqyg178c1n2ydscn5fb-zed-4`, which mirrors
  `helixml/zed`. The `upstream` remote (`https://github.com/zed-industries/zed.git`,
  read-only) is **not** configured in fresh clones and must be added. Fork default
  branch here is **`main`** (not `helix-fork`).
- **Living porting guide**: `/home/retro/work/zed/portingguide.md` — the canonical
  merge log with per-merge dated `## Merge NNN` sections. *This* is the file to
  update continuously. (See Open Questions re: the task brief's alternative path.)
- **Helix platform repo**: `/home/retro/work/helix/` — `sandbox-versions.txt`
  carries `ZED_COMMIT=`, bumped to the new merge HEAD after the Zed branch is pushed.
  Canonical builder: `./stack build-zed dev` (Docker; no local Rust toolchain).

## User Stories

### 1. Platform Engineer (performing the merge)
> As a platform engineer, I want to absorb the latest upstream catch-up window
> while preserving every Helix customisation, so the fork does not fall further
> behind and future merges stay small.

### 2. Helix User
> As a Helix user, I want upstream's newest bug fixes, sandboxing/performance
> improvements, and ACP protocol advances — without losing WebSocket sync, draft
> suppression, headless mode, Codex/Claude ACP routing, or any active Critical Fix.

### 3. Future Merge Engineer
> As the engineer running the next merge, I want `portingguide.md` updated **as
> each conflict is resolved** with a dated `## Merge 002265 (2026-07-20)` entry
> documenting every conflict, every retired-because-upstream-absorbed patch, and
> any ACP-signature repair — because an undocumented window is unmergeable next time.

## Acceptance Criteria

### Merge Completeness
- [ ] `git log` confirms fork main (via the feature branch) contains all upstream
      commits through the measured upstream HEAD; any skipped commit is explicitly
      justified in `portingguide.md`
- [ ] Merge (not squash, not rebase) — `git merge upstream/main` preserving history
- [ ] All Helix-only commits on fork main since the last fence are preserved verbatim
      on the merge branch (enumerate them at runtime; e.g. the post-002100 PRs
      #65 crash-frame, #66 unify-agent-send + prompt-queue E2E phases, #67 Codex-ACP,
      plus anything landed since)

### ACP Protocol (verify version at runtime)
- [ ] `Cargo.toml` takes upstream's `agent-client-protocol` pin (do not keep an
      older version); `Cargo.lock` accepted from upstream and regenerated by build
- [ ] If the ACP crate bumped this window, all `AgentConnection` / ACP-trait impls
      in `agent_servers/src/acp.rs`, `agent/src/`, `acp_thread/` compile against it;
      non-exhaustive struct literals converted to builders; `ErrorCode` arms checked
- [ ] Every ACP-signature repair forced by a bump is documented in `portingguide.md`
- [ ] Escalate to the user if any ACP change is not a mechanical fix (unclear
      lifecycle/semantics), rather than guessing

### Critical Fix Preservation (`portingguide.md` §"Critical Fixes")
- [ ] Fix #1: `NativeAgent` clone / `pending_sessions` shared-task in `load_session()`
- [ ] Fix #2: no duplicate WebSocket sends from `agent_ui/src/acp/thread_view.rs`
- [ ] Fix #3: `content_only()` strips `## Assistant` heading
- [ ] Fix #4: `notify_thread_display()` for follow-ups to non-visible threads
- [ ] Fix #5: stale pending entries flushed when a different entry starts streaming
- [ ] Fix #6: every `send()` emits exactly one `Stopped` (`stopped_emitted_for_task`)
- [ ] Fix #7: `THREAD_REGISTRY` unregistration on entity replacement
- [ ] Fix #8: `cancel()` drops `send_task` instead of awaiting it
- [ ] Fix #9: `stopped_emitted_for_task` guard on the normal-completion path
- [ ] Fix #11: entity-identity guard in `agent_panel.rs` `load_agent_thread`
- [ ] `cargo test -p acp_thread test_second_send` passes (Fix #6 invariant), if local Rust

### Helix-Specific Surface (must survive — brief constraints 1–13)
- [ ] `crates/external_websocket_sync/` intact (all source), incl. any recent PRs
      (crash-terminal-frame, Codex-ACP routing, unified agent-message sending)
- [ ] `send_agent_ready`, `wait_for_websocket_connected`, UI-state-query callback,
      `acp_history_store()` in `agent_panel.rs`
- [ ] `ThreadDisplayNotification` handler still calls `OnboardingUpsell::set_dismissed(true, cx)`
      and initialises `NativeAgentSessionList` (brief #9, #10)
- [ ] Fix 1b cfg-gated draft-suppression `return;` is still the FIRST statement of
      its `BaseView::Uninitialized` branch (re-locate — `agent_panel.rs` churns heavily)
- [ ] `from_existing_thread()` field set matches its `::new` sibling (recurring
      signature-drift site) on `ConversationView` / `AcpServerView`
- [ ] `AcpBetaFeatureFlag::enabled_for_all() -> true` override intact (brief #7)
- [ ] `trust_all_worktrees: true`, `show_sign_in: false`, and branding/onboarding
      settings intact in `assets/settings/default.json` (brief #8, #12)
- [ ] Built-in agent hiding (Claude Code / Codex / Gemini) stays wrapped in
      `cfg(not(feature = "external_websocket_sync"))` (brief #11)
- [ ] Windowless `cx.subscribe()` in `thread_service.rs` preserved so `message_added`
      streams incrementally, not only at completion (brief #6, #13)
- [ ] `--allow-multiple-instances` + `--headless` + `initialize_headless()` +
      `build_application(headless)` in `crates/zed/src/main.rs`
- [ ] `title_bar` `external_websocket_sync` dep `optional = true` + `render_restricted_mode`
      cfg gate; `rust-embed` `debug-embed` feature; `wait_for_tools_ready` uses
      `cx.background_executor().timer()` (not `smol::Timer`)
- [ ] 3× `// HELIX: External agent` bypass markers in `extensions_ui.rs`
- [ ] `grep_tool.rs` 001410 `truncate_long_lines` patch reconciled with upstream churn
- [ ] `BaseView` / `ContextServerStatus` (and any new ACP status) matches stay exhaustive

### Build & Test (hard gates)
- [ ] `cd /home/retro/work/helix && ./stack build-zed dev` succeeds — zero errors
- [ ] `cargo check` passes **both** with and without `--features external_websocket_sync`
      (compilation must pass with the feature on and off — brief #4)
- [ ] `.drone.yml` still builds with `--locked` (brief #5 — mandatory; source is
      mounted read-only in CI, so an unlocked build fails)
- [ ] (if local Rust) `cargo test -p external_websocket_sync` full pass;
      `cargo test -p agent_servers test_concurrent_session_creation_is_serialized`
- [ ] **External WebSocket sync E2E (Docker) — HARD GATE.** All phases green.
      The task brief describes 4 named phases (see below); the established runner
      enumerates more — **enumerate the actual phase list at runtime**, do not
      assume a count. `go mod tidy` in `e2e-test/helix-ws-test-server/` first.
- [ ] One retry permitted per agent for the known Claude Phase-1 npm-install / single
      API-latency flake

#### E2E phase gate (from task brief)
```
docker build -t zed-ws-e2e -f crates/external_websocket_sync/e2e-test/Dockerfile .
docker run --rm -e ANTHROPIC_API_KEY=... -e TEST_TIMEOUT=120 zed-ws-e2e
```
Must pass:
- Phase 1 — new thread: `agent_ready` → `chat_message` → `thread_created` → `message_completed`
- Phase 2 — follow-up on same thread: `entry_count` increases
- Phase 3 — new second thread created and switched to
- Phase 4 — message to non-visible Thread A (entity-released regression)
- UI-state queries: correct `thread_id`, `entry_count`, `active_view`

(The repo's `run_docker_e2e.sh` is the established wrapper and covers these plus the
newer prompt-queue / cancel / streaming-incremental phases. Run it for both
`zed-agent` and `claude`.)

### Documentation (hard gate — written incrementally)
- [ ] `portingguide.md` updated **as each conflict is resolved**, not at the end
- [ ] New `## Merge 002265 (2026-07-20)` section at the top of the merge-history list
- [ ] Per-conflict subsection under `### Conflicts and Resolutions`
- [ ] `### ACP bump — repaired sites` (or explicit "no ACP bump this window")
- [ ] Explicit retirement notes for any Helix patch absorbed upstream (or "none")
- [ ] Helix-surface survival-check subsection (per-area confirmation)
- [ ] Commit-history table extended; stale entries corrected/deleted; new
      Rebase-Checklist entry only if a genuinely new recurring gotcha is confirmed

### Process
- [ ] Feature branch `feature/002265-merge-latest-zed` created from current fork main
- [ ] Branch pushed to `helixml/zed` (via `origin`)
- [ ] Helix `sandbox-versions.txt` `ZED_COMMIT` bumped to the new merge HEAD on a
      `feature/002265-merge-latest-zed` branch in the helix repo, pushed
- [ ] `pull_request_zed.md` + `pull_request_helix.md` written into this task directory
- [ ] `main` is **not** force-pushed without explicit user approval; the Helix UI
      handles PR creation — no agent-initiated PRs
- [ ] Re-fetch `upstream/main` + `origin/main` before declaring done; absorb any
      out-of-band fork-main pushes; target a newer upstream HEAD or run an extension
      round if upstream advanced materially mid-work

## Out of Scope
- Net-new Helix feature development
- Modifying E2E assertions unless a legitimate upstream ACP API change strictly requires it
- Upstreaming Helix patches back to `zed-industries/zed`
- Refactors of Helix crates beyond what conflict resolution / an ACP bump forces
- Adopting upstream slash-command thread-controls, elicitation UI, or sandboxing UX
  into Helix-mode flows beyond keeping them compiling
- Rewriting the porting guide from scratch — amend and extend in place

## Open Questions

- **Porting-guide path discrepancy (please confirm).** The task brief names
  `design/2026-02-07-zed-fork-rebase-to-upstream.md` (which exists in the *helix*
  repo, is the 638-line historical one-time port narrative, and has **no** per-merge
  `## Merge NNN` sections). Every recent merge (002224, 002251, …) instead updates
  the living `/home/retro/work/zed/portingguide.md`. **This spec assumes the living
  `zed/portingguide.md` is the correct target** and the brief's path is a stale
  reference to the historical doc. Confirm — or say if both should be touched.
- **Branch / repo layout.** The brief specifies repo `/prod/home/luke/pm/zed-upstream`
  on branch `helix-fork` with remotes `helix`/`origin`. The sandbox reality is
  `/home/retro/work/zed` off `origin/main` with `upstream` added. Assumption: work
  in the sandbox layout, push to `origin` (which mirrors `helixml/zed`). Confirm.
- **Actual window size / ACP state.** Unmeasurable at planning time (empty clone).
  Did 002251 land on fork main? Has `agent-client-protocol` bumped again past `1.1.0`?
  The implementer measures at runtime; risk profile follows from that.
- **E2E phase count.** The brief lists 4 phases; the repo's `run_docker_e2e.sh`
  enumerates more (prompt-queue, cancel, streaming-incremental). Assumption: pass
  the brief's 4 named phases *and* every phase the current runner enumerates, for
  both `zed-agent` and `claude`. Confirm if only the 4 are required.
