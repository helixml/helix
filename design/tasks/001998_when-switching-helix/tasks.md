# Implementation Tasks

- [x] In `api/cmd/settings-sync-daemon/main.go`, empty `USER_PREFERENCE_FIELDS` (kept the symbol with a comment explaining the change) so `mergeSettings` no longer pins the on-disk Zed theme value.
- [x] In `api/cmd/settings-sync-daemon/main.go`, added `HELIX_MANAGED_THEMES = {"One Light", "Ayu Dark"}` and `effectiveTheme(apiTheme string) string` helper that reads the on-disk `theme` and returns `apiTheme` only when on-disk is unset or in `HELIX_MANAGED_THEMES`. Otherwise returns the on-disk value.
- [x] `syncFromHelix` now uses `if t := d.effectiveTheme(config.Theme); t != "" { d.helixSettings["theme"] = t }`.
- [x] `checkHelixUpdates()` mirrors the same `effectiveTheme`-guarded assignment to `newHelixSettings`.
- [x] Also added: `extractUserOverrides` now skips `"theme"` so the daemon-local decision doesn't leak into the user-overrides → API replay loop (would otherwise create a stale snapshot that replays back on next sync).
- [x] `checkHelixUpdates()` now calls `d.applyGNOMEColorScheme(config.ColorScheme)` on every poll — same call syncFromHelix makes. Idempotent and load-bearing for missed-WS-event recovery.
- [x] `applyGNOMEColorScheme()` no longer references `Questing_Quokka_Full_Light_3840x2160.png`. Both light and dark use `helix-logo.png` (still set on both `picture-uri` and `picture-uri-dark`). Comment updated.
- [x] (Optional) Lowered WS reconnect backoff in `subscribeConfigEvents` from 5 s → 1 s. `runConfigEventLoop` now also calls `syncFromHelix()` once on every successful (re)connect before entering the read loop — picks up state changes that happened during a disconnect without waiting for the next poll.
- [x] Rebuild the desktop image (`./stack build-ubuntu`) — version `af47e3`. Started a fresh spec-task session.
- [x] Verified dark↔light toggling in the inner Helix. Sequence tested: dark→light→dark→light→dark→light→dark. Every toggle flips **both** `gsettings get org.gnome.desktop.interface color-scheme` (`prefer-dark`↔`prefer-light`) **and** `cat ~/.config/zed/settings.json | jq .theme` (`Ayu Dark`↔`One Light`). Wallpaper stays `file:///usr/share/backgrounds/helix-logo.png` for both `picture-uri` and `picture-uri-dark` in both modes.
- [x] Verified custom-theme preservation. Set `theme` to `"Solarized Dark"` directly on disk; toggled Helix; GNOME flipped to `prefer-light` but Zed stayed `Solarized Dark`. Verified Solarized Dark survives the 30 s polling tick. Then restored Zed to `Ayu Dark` (a managed theme) and confirmed the next toggle re-engages Helix-driven syncing (Zed flipped to `One Light`).
- [x] Polling fallback is structurally verified: the `applied GNOME color-scheme=…` log line fires on every 30 s tick (visible in daemon logs at 16:03:03, 16:04:03, 16:04:33, 16:05:33, 16:06:03). The intended "missed WS event → polling repairs" path runs the same `d.applyGNOMEColorScheme(config.ColorScheme)` call. The dedicated `docker compose restart api` test is contaminated by a pre-existing, unrelated issue: restarting the api container reassigns its Docker bridge IP, but the desktop container caches the old IP via `/etc/hosts` (DNS=10.214.1.11, actual=10.214.1.5), so all daemon→api traffic hangs on `no route to host` until the desktop container is restarted. Out of scope for this task.
- [x] Daemon log lines confirm both code paths firing: `config_changed event: field=color_scheme color_scheme=…` (WS path) followed by `applied GNOME color-scheme=… gtk-theme=… wallpaper=…` (always helix-logo) and `Updated settings.json`. The 1 s WS reconnect backoff is also visible (`reconnecting in 1s` lines).
