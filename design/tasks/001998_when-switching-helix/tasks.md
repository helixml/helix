# Implementation Tasks

- [~] In `api/cmd/settings-sync-daemon/main.go`, remove `"theme"` from `USER_PREFERENCE_FIELDS` (line 945-947) so `mergeSettings` no longer pins the on-disk Zed theme value. Update the leading comment to reflect that theme is now governed by `HELIX_MANAGED_THEMES` + `effectiveTheme()`.
- [~] In `api/cmd/settings-sync-daemon/main.go`, add a `HELIX_MANAGED_THEMES` set (`{"One Light", "Ayu Dark"}`) and a helper `effectiveTheme(apiTheme string) string` that reads the on-disk `theme` and returns `apiTheme` only when on-disk is unset or in `HELIX_MANAGED_THEMES`. Otherwise returns the on-disk value, preserving any custom theme the user picked in Zed's UI.
- [~] In `syncFromHelix` (line 784-786), replace the unconditional theme assignment with `if t := d.effectiveTheme(config.Theme); t != "" { d.helixSettings["theme"] = t }`.
- [~] In `checkHelixUpdates()` (~line 1424), add the same `effectiveTheme`-guarded theme assignment to `newHelixSettings`. Update the "Note: theme is a USER_PREFERENCE_FIELD" comment to match.
- [ ] In `api/cmd/settings-sync-daemon/main.go` `checkHelixUpdates()`, call `d.applyGNOMEColorScheme(config.ColorScheme)` on every poll so the 30 s polling fallback also repairs GNOME state when a WebSocket `config_changed` event was missed.
- [ ] In `api/cmd/settings-sync-daemon/main.go` `applyGNOMEColorScheme()` (line 908-931), remove the light-mode wallpaper override (`Questing_Quokka_Full_Light_3840x2160.png`) — keep `file:///usr/share/backgrounds/helix-logo.png` for both light and dark. Update the leading comment.
- [ ] (Optional) Lower the WS reconnect backoff in `subscribeConfigEvents` from 5 s → 1 s, and call `syncFromHelix()` once on every successful WS reconnect (before the read loop) so changes that happened during a disconnect are picked up immediately rather than waiting for the next poll.
- [ ] Rebuild the desktop image (`./stack build-ubuntu`) and start a **new** spec-task session in the inner Helix — settings-sync-daemon does not hot-reload.
- [ ] Verify in the inner Helix at `http://localhost:8080`: register/onboard, start a session, then toggle dark↔light multiple times in the top bar. After each toggle, inspect both surfaces inside the desktop container: `gsettings get org.gnome.desktop.interface color-scheme` and `cat /home/retro/.config/zed/settings.json | jq '.theme'`. Confirm both flip in both directions, repeatedly. Confirm wallpaper is `helix-logo.png` in both modes.
- [ ] Verify the custom-theme preservation: with the session running, manually edit `~/.config/zed/settings.json` (or pick via Zed's UI) to set `theme` to something outside the managed set (e.g. `"Solarized Dark"`). Toggle Helix dark↔light. Confirm GNOME flips but `~/.config/zed/settings.json` `.theme` stays `"Solarized Dark"`.
- [ ] Force-test the polling fallback: with a session running, `docker compose restart api`, immediately toggle, wait ≤30 s, confirm both GNOME and Zed converge to the new theme.
- [ ] Tail `docker logs <ubuntu-external-container>` during the test for `applied GNOME color-scheme=…` and `Updated settings.json` lines to confirm both code paths fire.
