# Requirements

## User Story

As a user, I want the agent to say "hello" and then stop, so I can verify the agent loop is working.

## Acceptance Criteria

- Agent outputs the greeting "hello" (case-insensitive variants acceptable).
- Agent performs no other actions after the greeting.
- Agent terminates its turn cleanly with no errors.
