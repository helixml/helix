# Align subscription status text styling

## Summary
Updated the active subscription details on the onboarding page to use the same typography size and text color as the "Subscription is active." status. This makes the font presentation consistent and gives the subscription details the same contrast ratio as the surrounding status text.

## Testing
Ran `yarn tsc`; the frontend TypeScript compilation completed successfully.
