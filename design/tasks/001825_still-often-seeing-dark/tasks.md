# Implementation Tasks

- [ ] Add `& a` link styling (color `#00d5ff`, no text-decoration, underline on hover, pinned visited color) to `.markdown-body` in `DesignReviewContent.tsx` (~line 1375)
- [ ] Add matching `& a` link styling to the markdown container `sx` in `DesignDocPage.tsx` (~line 222)
- [ ] Build frontend (`cd frontend && yarn build`) to verify no errors
- [ ] Test in browser: open a spec review containing links, confirm teal color on dark background
