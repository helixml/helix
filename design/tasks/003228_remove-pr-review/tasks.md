# Implementation Tasks: Remove PR Review Coordinator Bootstrap Overhead

- [ ] Fetch current bot content: `helix api /orgs/helix/bots/b-pr-coordinator > /tmp/prc-current.json` and save `.bot.content` to `/tmp/prc-content.md`
- [ ] Edit the prompt in place per design.md deltas: remove `read_events` step and `last_event_id` state; remove the post-create `update_spectask` FINISH-STEP call and embed FINISH-STEP (with `"$HELIX_SPEC_TASK_ID"`) in the PR Review Brief description; add `sandbox_runtime: "headless-ubuntu"` to the `create_spectask` dispatch; replace every `gh` usage in coordinator procedure, task bookkeeping, and PR Review Brief with the `get_secret` + `curl` + `jq` REST recipes (health, PR metadata, files, diff, existing reviews, POST review with `commit_id`); add "never install or invoke `gh`" and token-hygiene lines; keep skip_planning, name pattern, priority, start_spectask_planning, self-loop and duplicate (PR, head SHA) guards, concurrency cap, i-have-adhd review style, ask_human fallbacks, and event filtering text unchanged
- [ ] Validate the draft locally: grep it for forbidden patterns (`gh `, `read_events`, `update_spectask`) and required markers (`headless-ubuntu`, `$HELIX_SPEC_TASK_ID`, `skip_planning`, `commit_id`, `api.github.com`) — all forbidden absent, all required present
- [ ] PATCH the bot: `helix api -X PATCH /orgs/helix/bots/b-pr-coordinator --input @/tmp/prc-patch.json` (build with `jq -n --rawfile`), then `helix api /orgs/helix/bots/b-pr-coordinator` and diff the returned `content` against the draft
- [ ] Restart the bot: `helix org bots restart b-pr-coordinator --org helix` and confirm it comes back running
- [ ] Trigger a test PR (this task's own implementation PR or a trivial throwaway PR on `helixml/helix`; not authored by the reviewer login, not a draft) and wait for the coordinator's next activation
- [ ] Verify the dispatch: coordinator bot log shows the trigger processed directly with NO `read_events` and NO `update_spectask` calls, and `create_spectask` called with `sandbox_runtime: "headless-ubuntu"`; created task is `pr-review-<N>-<short>` with `skip_planning` and a description containing the FINISH-STEP line
- [ ] Verify the review task: runs on OpenCode in a headless sandbox, performs the review via REST only, and POSTs a formal review with `commit_id` equal to the PR head SHA — confirm via `curl https://api.github.com/repos/helixml/helix/pulls/<N>/reviews | jq` that the review exists, is authored by the coordinator's reviewer login, and carries the correct `commit_id`; confirm the token value appears nowhere in the review body, task description, or logs
- [ ] Verify FINISH-STEP: the review task moved itself to `done` (board or task status)
- [ ] Record the test PR URL, review task URL, and review URL in this task's design doc and final report
