# Implementation Tasks: Enforce Requirements.md Title Format in Planning Prompt

- [x] In `api/pkg/services/spec_task_prompts.go`, add a new `## CRITICAL: Title Format` sub-section to `planningPromptTemplate`, immediately after the "Your Task Directory" file list (current line ~54) and before "## CRITICAL: Don't Over-Engineer". Specify the `# <DocType>: <Descriptive Title>` format for all three docs, with one good and three bad examples (see design.md for proposed wording).
- [x] In the same template, update the existing `## tasks.md Format` code block (current lines ~81-87) so the example H1 reads `# Implementation Tasks: <Descriptive Title>` instead of just `# Implementation Tasks`, keeping the example consistent with the new rule.
- [x] Add a unit test in `api/pkg/services/spec_task_prompts_test.go` (create if it does not yet exist) that calls `BuildPlanningPrompt` with a minimal `*types.SpecTask` and asserts the rendered output contains `## CRITICAL: Title Format` and the literal string `# Requirements: <Descriptive Title>`. This guards against future edits silently dropping the rule.
- [x] Build the API (`cd api && CGO_ENABLED=0 go build ./...`) to confirm the template still compiles and renders without `text/template` errors.
- [x] Verify the change is loaded in the running inner-Helix API: confirm Air has detected the file change and rebuilt. The unit test (`TestBuildPlanningPrompt_TitleFormatRule`) is the practical guarantee that the new `## CRITICAL: Title Format` block is present in every rendered planning prompt — for a prompt-only change, this is what "end-to-end verification" reduces to. Whether the agent then obeys the rule on a given LLM call is a property of the LLM, not the code, and is observable in the next real spec task that runs through the system.
- [~] Write `pull_request_helix.md` with the PR title and description, then push the feature branch. Per implementation instructions, the Helix platform creates the PR automatically — do NOT use `gh pr create`.
