# Implementation Tasks: Stop False "[Agent did not respond]" When Agent Streams Past 2-Minute Timer

- [x] Hoist the error string into a package-level `const CommentTimerNoResponseMessage = "[Agent did not respond - try sending your comment again]"` in `api/pkg/server/spec_task_design_review_handlers.go` and replace the two literal occurrences (timer at line ~833, tests/UI any references).
- [x] Refactor the `time.AfterFunc` closure body at `api/pkg/server/spec_task_design_review_handlers.go:818` into a named method `(s *HelixAPIServer) handleCommentTimeout(ctx context.Context, sessionID, commentID string)` so it can be unit-tested without spinning a real timer. Closure becomes a thin wrapper that calls the named method. No behavior change yet.
- [x] In `handleCommentTimeout`, after the existing `currentComment.RequestID != "" && currentComment.AgentResponse == ""` check passes, load the linked interaction via `s.Store.GetInteraction(ctx, currentComment.InteractionID)` and call `types.TextFromInteraction(...)`. If text is non-empty OR `interaction.State` is `Complete`/`Interrupted`/`Error`, log a skip and return without stamping the error. (AC-1, AC-3)
- [x] In `finalizeCommentResponse` at `api/pkg/server/spec_task_design_review_handlers.go:1233`, change the population guard from `if comment.AgentResponse == ""` to `if comment.AgentResponse == "" || comment.AgentResponse == CommentTimerNoResponseMessage`. When overwriting the error string, log at warn level with comment_id, interaction_id, and new response length. Apply the same change to the `populateAgentResponseFromSession` call gate just below. (AC-2)
- [x] Switch the timer closure's captured `ctx` to `context.Background()` so the deferred timer body never reads from a request-scoped context that may already be cancelled. (Defensive — flag in PR.)
- [x] Add `TestCommentTimer_SkipsErrorWhenInteractionHasContent` to `api/pkg/server/spec_task_design_review_handlers_test.go`: mock store returns a comment with empty `AgentResponse` and an interaction whose `ResponseMessage` is non-empty + state `Waiting`. Call `handleCommentTimeout`; assert NO `UpdateSpecTaskDesignReviewComment` call with the error string is made.
- [x] Add `TestCommentTimer_StampsErrorWhenInteractionEmpty`: same setup but interaction has empty response and state `Waiting`. Assert the error IS stamped (regression guard).
- [x] Add `TestFinalize_OverwritesStaleTimerError`: comment row's `AgentResponse` already equals `CommentTimerNoResponseMessage`, interaction has real text. Call `finalizeCommentResponse`; assert final `AgentResponse` is the real text. Also added `TestHandleCommentTimeout_SkipsErrorWhenInteractionIsTerminal`, `TestHandleCommentTimeout_NoopWhenAlreadyResolved`, `TestFinalizeCommentResponse_PopulatesEmptyComment`.
- [x] Run `CGO_ENABLED=1 go test -v -run TestCommentTimerSuite ./api/pkg/server/ -count=1` locally (all 6 pass), full WebSocketSync suite passes, `go build ./pkg/server/ ./pkg/store/ ./pkg/types/` clean.
- [x] E2E in inner Helix at `http://localhost:8080` — **SKIPPED**: inner Helix is not running in this sandbox (`curl http://localhost:8080` returns no response; `docker ps` empty). The 6-test `CommentTimerSuite` covers all decision branches of `handleCommentTimeout` and `finalizeCommentResponse` directly against the real production code with mocked store calls, including the sticky-error repair path. Reviewer should validate E2E in a live environment before merge.
- [x] E2E for sticky-error repair — **SKIPPED** (see above): covered by `TestFinalizeCommentResponse_OverwritesStaleTimerError` which exactly mirrors the SQL-injection scenario in the unit test.
- [x] Push code to `feature/002046-stop-false-agent-did-not`. PR description in `pull_request_helix.md`. User triggers PR creation via the Helix UI.
