# Implementation Tasks: Stop False "[Agent did not respond]" When Agent Streams Past 2-Minute Timer

- [ ] Hoist the error string into a package-level `const CommentTimerNoResponseMessage = "[Agent did not respond - try sending your comment again]"` in `api/pkg/server/spec_task_design_review_handlers.go` and replace the two literal occurrences (timer at line ~833, tests/UI any references).
- [ ] Refactor the `time.AfterFunc` closure body at `api/pkg/server/spec_task_design_review_handlers.go:818` into a named method `(s *HelixAPIServer) handleCommentTimeout(ctx context.Context, sessionID, commentID string)` so it can be unit-tested without spinning a real timer. Closure becomes a thin wrapper that calls the named method. No behavior change yet.
- [ ] In `handleCommentTimeout`, after the existing `currentComment.RequestID != "" && currentComment.AgentResponse == ""` check passes, load the linked interaction via `s.Store.GetInteraction(ctx, currentComment.InteractionID)` and call `types.TextFromInteraction(...)`. If text is non-empty OR `interaction.State` is `Complete`/`Interrupted`/`Error`, log a skip and return without stamping the error. (AC-1, AC-3)
- [ ] In `finalizeCommentResponse` at `api/pkg/server/spec_task_design_review_handlers.go:1233`, change the population guard from `if comment.AgentResponse == ""` to `if comment.AgentResponse == "" || comment.AgentResponse == CommentTimerNoResponseMessage`. When overwriting the error string, log at warn level with comment_id, interaction_id, and new response length. Apply the same change to the `populateAgentResponseFromSession` call gate just below. (AC-2)
- [ ] Switch the timer closure's captured `ctx` to `context.Background()` so the deferred timer body never reads from a request-scoped context that may already be cancelled. (Defensive — flag in PR.)
- [ ] Add `TestCommentTimer_SkipsErrorWhenInteractionHasContent` to `api/pkg/server/spec_task_design_review_handlers_test.go`: mock store returns a comment with empty `AgentResponse` and an interaction whose `ResponseMessage` is non-empty + state `Waiting`. Call `handleCommentTimeout`; assert NO `UpdateSpecTaskDesignReviewComment` call with the error string is made.
- [ ] Add `TestCommentTimer_StampsErrorWhenInteractionEmpty`: same setup but interaction has empty response and state `Waiting`. Assert the error IS stamped (regression guard).
- [ ] Add `TestFinalize_OverwritesStaleTimerError`: comment row's `AgentResponse` already equals `CommentTimerNoResponseMessage`, interaction has real text. Call `finalizeCommentResponse`; assert final `AgentResponse` is the real text.
- [ ] Run `CGO_ENABLED=1 go test -v -run 'TestCommentTimer|TestFinalize' ./api/pkg/server/ -count=1` locally, then `cd api && go build ./pkg/server/ ./pkg/store/ ./pkg/types/`. Push and check Drone CI.
- [ ] E2E in inner Helix at `http://localhost:8080` per design.md §"E2E verification": create a spec task, comment with a prompt that streams 3+ minutes, confirm no false error appears and the final response is the real one. Capture screenshots at t=2:00 and at completion.
- [ ] E2E for sticky-error repair: while agent is streaming, run `docker exec helix-postgres-1 psql -U postgres -d postgres -c "UPDATE spec_task_design_review_comments SET agent_response = '[Agent did not respond - try sending your comment again]' WHERE id = '<id>';"`, wait for `message_completed`, confirm comment row is overwritten with real response.
- [ ] Push commit, open PR titled `fix(api): stop false "did not respond" timer while agent is still streaming`, link to `helix-specs` task `002046_i-still-see-agent-did`, paste the E2E screenshots in the PR body.
