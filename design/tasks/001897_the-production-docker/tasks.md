# Implementation Tasks

- [x] Remove the `ports:` block (lines 124–125) from the `vectorchord-kodit` service in `docker-compose.yaml`
- [x] Verify `docker compose config` parses cleanly after the change
