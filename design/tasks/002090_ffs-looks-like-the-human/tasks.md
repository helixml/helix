# Implementation Tasks: Worker Activation Must Reuse the Project's Human Desktop Session

- [x] Read the call-site list at the bottom of `design.md` (sections 1-10) in order. Don't start coding until you've actually opened each file — the guard is small but lives in a flow with five collaborators, and skipping the read is how the next regression lands.
- [x] Add `api/pkg/server/exploratory_session_activation_test.go` with the four cases from `design.md` "Test Plan" (regression gate, status-pill flip on Resume, no project → no reuse, non-exploratory role → no reuse). Suite-based, mirrors `start_dev_container_test.go` style. Mock executor via `external_agent.MockExecutor`.
- [x] Confirm the new test file **fails on current `main`** before changing any production code. Case 1 fails with `expected call ... GetSession(... ses_existing_exploratory) doesn't match the argument at index 1, Got: ses_01... (string)` plus a missing `GetProjectExploratorySession` call — i.e. the production code mints a fresh id and never asks for the existing exploratory row. Cases 2-4 pass (they pin paths unaffected by the guard).
- [x] In `api/pkg/server/session_handlers.go` `StartExternalAgentSession` (line 2421), insert the guard from `design.md` "Production Change": pre-resolve `sessionID` from `GetProjectExploratorySession` when `req.SessionRole == "exploratory" && req.ProjectID != ""`, fall back to `system.GenerateSessionID()` otherwise.
- [x] In the same handler, the persistence branching collapsed into a single path because `Controller.WriteSession` already calls `Store.UpdateSession` which is a GORM `Save` (upsert) — the reused-row case and the fresh-row case both flow through it. Likewise `CreateInteractions` uses `OnConflict{UpdateAll: true}`, so re-writing existing interactions is a no-op. The implementation is: build `session` from `existing` when reusable (preserves owner/org/metadata/parent_app), else build the fresh struct; then run the unchanged `appendOrOverwrite` → `WriteSession` → `WriteInteractions` → `StartDesktop` path. One code path, two sources for the starting struct.
- [x] Re-run the new test file — all four cases PASS after the guard is in.
- [ ] (Skipped — server-side guard fully closes the regression test on its own.) The spawner-side belt-and-braces from `design.md` "Spawner-side belt-and-braces". Defence in depth, not load-bearing. Can be added in a follow-up if a future code path bypasses `StartExternalAgentSession`.
- [x] `go build ./api/pkg/server/...` clean. `go test ./api/pkg/server/...` green (1.26s). `go test ./api/pkg/org/infrastructure/runtime/helix/...` green (0.11s).
- [ ] Walk through the manual repro in `requirements.md` §C against the inner Helix at `http://localhost:8080`. Steps 1-5: open Human Desktop, hire AI Worker, reload SpecTasks, confirm View Human Desktop / running, run the SQL one-row check. Don't skip the SQL check — the UI showing "running" with two DB rows is the failure mode that hid this bug between releases. Save before/after screenshots into `screenshots/`.
- [ ] Commit on a feature branch `feature/002090-human-desktop-activation-reuse`. Commit message must reference: this task (`Spec-Ref: helix-specs@<sha>:002090_ffs-looks-like-the-human`), the regression-test-first ordering, and the load-bearing guard line range. Push, open PR, watch CI via the Drone MCP tools, fix anything red.
