# Implementation Tasks: Worker Activation Must Reuse the Project's Human Desktop Session

- [ ] Read the call-site list at the bottom of `design.md` (sections 1-10) in order. Don't start coding until you've actually opened each file — the guard is small but lives in a flow with five collaborators, and skipping the read is how the next regression lands.
- [ ] Add `api/pkg/server/exploratory_session_activation_test.go` with the four cases from `design.md` "Test Plan" (regression gate, status-pill flip on Resume, no project → no reuse, non-exploratory role → no reuse). Suite-based, mirrors `start_dev_container_test.go` style. Mock executor via `external_agent.MockExecutor`.
- [ ] Confirm the new test file **fails on current `main`** before changing any production code. The case-1 message should read approximately `expected 1 exploratory row for project P, got 2`. If it passes on `main`, your fake setup is wrong — the test is supposed to repro the bug, not paper over it. Re-read the bug description in `requirements.md` and adjust the setup until the failure mode matches.
- [ ] In `api/pkg/server/session_handlers.go` `StartExternalAgentSession` (line 2421), insert the guard from `design.md` "Production Change": pre-resolve `sessionID` from `GetProjectExploratorySession` when `req.SessionRole == "exploratory" && req.ProjectID != ""`, fall back to `system.GenerateSessionID()` otherwise.
- [ ] In the same handler, branch the persistence: when `sessionID` was reused (i.e. matched an existing row id), call `UpdateSession` + `WriteInteractions` so the new prompt appends to the existing session; when it was minted fresh, keep the existing `WriteSession` + `WriteInteractions` path. Preserve owner/org/metadata/parent_app on the reused row — only update fields the new request actually carries (model, system prompt if non-empty, etc.).
- [ ] Re-run the new test file — case 1 must now pass with `count == 1` and `session.ID == A.ID`. Re-run cases 2-4 — they should be green from the start (they pin the unchanged paths) but rerun anyway to catch any incidental breakage.
- [ ] (Optional, lower priority) Add the spawner-side belt-and-braces from `design.md`: in `api/pkg/org/infrastructure/runtime/helix/spawner.go` `ensureSession`, pre-fill `SendPromptParams.SessionID` from `c.Client.ExploratorySession(ctx, state.ProjectID)` when `state.SessionID == ""`. Add a `fakeHelixClient` case in `spawner_test.go` asserting that with `f.exploratorySession` pre-seeded, `EnsureAndSend` calls `SendMessage` (not `StartSession`). Skip this if the server-side guard fully closes the regression test — land separately if it does.
- [ ] `go build ./api/...` clean. `go test ./api/pkg/server/... ./api/pkg/org/infrastructure/runtime/helix/...` all green (suite-based, expect ~30s).
- [ ] Walk through the manual repro in `requirements.md` §C against the inner Helix at `http://localhost:8080`. Steps 1-5: open Human Desktop, hire AI Worker, reload SpecTasks, confirm View Human Desktop / running, run the SQL one-row check. Don't skip the SQL check — the UI showing "running" with two DB rows is the failure mode that hid this bug between releases. Save before/after screenshots into `screenshots/`.
- [ ] Commit on a feature branch `feature/002090-human-desktop-activation-reuse`. Commit message must reference: this task (`Spec-Ref: helix-specs@<sha>:002090_ffs-looks-like-the-human`), the regression-test-first ordering, and the load-bearing guard line range. Push, open PR, watch CI via the Drone MCP tools, fix anything red.
