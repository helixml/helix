# Implementation Tasks: Give Spawned Spec Tasks Their Agent's Tool Access

- [x] Add `BoundAgentForProject(ctx, st *store.Store, orgID, projectID) (orgchart.NodeID, error)` and `AgentToolNames(ctx, st *store.Store, orgID, nodeID) ([]tool.Name, error)` to `api/pkg/org/infrastructure/runtime/helix`: Bot nodes via `Nodes.List` filtered `Kind==Bot`, home match via `LoadState().ProjectID`; exactly 1 → id; 0 → typed `ErrNoBoundAgent`; >1 → WARN log + same error; never match on `Node.ProjectIDs`. Nil-safe `Node.Tools` accessor.
- [x] Add the subject-resolution primitives: `WithBoundWorker`/`BoundWorkerFromContext` in `api/pkg/org/infrastructure/runtime/principal.go`, and `tool.SubjectForCaller(ctx, caller)` (Bot → itself; project principal → bound worker; unbound principal → typed clean error). Document the (a) on-behalf-of / (b) task-keyed / (c) unsupported-delegated classification in the helper doc.
- [x] `api/pkg/server/agent_tools_handlers.go` `specTaskAgentTools`: keep today's non-org nil behavior; after existing sanitize, when `BoundAgentForProject` resolves append deduped `AgentToolNames` (post-sanitize so registry org-tool names survive). Wire org-store access per the `server/helix_org.go` composition-root pattern. Do NOT touch external-agent/ or the mount/rev call sites — `len(specTaskTools) > 0` and `AgentToolsRev` consume this list already (`zed_config_handlers.go:193/538`).
- [x] `api/pkg/server/mcp_backend_spectask.go`: same union in `eligibleSpecTaskTools` (shared helper with the item above), and stash the resolved bound worker on ctx via `WithBoundWorker` before `ServeMCPForCaller`.
- [x] `api/pkg/org/interfaces/mcptools/get_secret.go` + `list_secrets.go`: resolve the worker id through `SubjectForCaller` (replaces the caller-ID-as-worker assumption for task callers); keep audit `Recorder` attribution to `inv.Caller.ID()` (task) + subject; Bot behavior byte-identical.
- [x] Identity audit PR deliverable: sweep every tool in `mcptools/builtins.go` reachable through any Bot's `Node.Tools`, mark it (a)/(b)/(c) in the PR description table, and convert (a)-class caller-identity reads to `SubjectForCaller` with per-class tests. No tool may widen beyond bound-Agent grants; (c) must error with zero DB writes. Block merge on the completed table.
- [x] Guardrail test: task planning sessions never carry `Metadata.OrgWorkerID`; a task API key still 403s on `/api/v1/mcp/helix-org` and serves tools on `/mcp/helix-tasks`.
- [x] Unit tests: helpers 0/1/2/allowlist-only/human; `specTaskAgentTools` table (non-org ≡ nil today; org unbound+allowlists ≡ today incl. rev; bound ⇒ union+mount+rev flip on agent-tools change; REST view equality); tools/list ≡ rev ≡ view; `get_secret`/`list_secrets` subject cases + audit checks; representatives `chat`/`managers` on-behalf, spec-task CRUD task-keyed, one (c)-class loud error; `BuildAgentToolsSection` golden-unchanged.
- [x] Build + test: `go build ./pkg/org/... ./pkg/server/ ./pkg/external-agent/`, `CGO_ENABLED=1 go test ./pkg/org/... ./pkg/server/ ./pkg/services/ -count=1`, `go test ./api/pkg/external-agent` for mount/rev behavior; openapi regen only if DTOs changed (expect none).
- [~] Inner-Helix e2e: hire Bot (default tools incl `chat`+`get_secret`) with a granted Helix secret; Bot-spawned task: tools/list = Bot set + task tools, `agent_tools` row shows nothing new, `chat` posts as the Agent with audit actor=task id, `get_secret` reads the value. UI-created task in same project: identical surface (silently entitled — expected now). `attach_tool` a new tool → restart task desktop → present (rev flip); detach → gone; fire the Bot → restart shows shrunk surface + loud clean org-call errors, full planning→approval→PR cycle still passes; unbound org project (this one) and a non-org task: zero observable change; second task in a different Bot's project resolves only that project's Agent.

- [x] (done inline) chat/managers delegation representatives + unbound loud-fail tests; org-admin block-list filter test; helix-org backend 403 suite.
