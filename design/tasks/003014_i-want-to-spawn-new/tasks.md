# Implementation Tasks: Grant Worker Secrets to Spawned Spec Tasks

- [ ] Add `mcptools.GetSecretName` and `ListSecretsName` to `SpecTaskAgentTools` in `api/pkg/org/interfaces/mcptools/defaults.go` (two data lines); confirm `sanitizeAgentTools`/`IsSpecTaskAgentTool` now keep the names everywhere.
- [ ] In `api/pkg/org/infrastructure/runtime/helix/spectasks.go` `Create`: append the two tool names to the new task's `AgentTools` (dedupe against existing entries) for every port-level Create including task-principal sub-task spawns; add a sentence to the `create_spectask` tool description in `mcptools/spec_tasks.go` noting the platform arms `list_secrets`/`get_secret` scoped to the caller's credentials at time of use. No schema, no args, REST creation untouched.
- [ ] Add `BoundAgentForProject(ctx, orgID, projectID) (orgchart.NodeID, error)` in `infrastructure/runtime/helix` (next to `LoadState`): `Nodes.List` → Bot filter → `LoadState(b).ProjectID == projectID`; exactly 1 → id; 0 → typed `ErrNoBoundAgent`; >1 → WARN + same error; ownership only (never `Bot.ProjectIDs`).
- [ ] Branch `mcptools/get_secret.go` + `list_secrets.go` on `ProjectPrincipalFromContext`: resolve owner via a `func(ctx, orgID, projectID) (NodeID, error)` seam on `mcptools.Deps`/`Config` (closure over the helper, wired in `server/helix_org.go`), read through the existing `workersecrets.Service` against that worker id, keep audit `Recorder` attributing to the task caller, and return distinct clean errors for unbound project / tool-not-on-task. Bot path byte-identical.
- [ ] Prompts: in `BuildAgentToolsSection` (`services/spec_task_prompts.go`) filter the two secret names out of the delegation enumeration/gate, and render the dedicated "armed with credential tools — list_secrets/get_secret" fragment iff they appear in the effective list; verify both call sites (`spec_driven_task_service.go:409`, `agent_instruction_service.go:646`) get it with no org-store access.
- [ ] Verify (no-code) consequences of the catalogue+stamp combo, asserting in tests: `specTaskAgentTools` (`server/agent_tools_handlers.go`) passes the pair for armed tasks; `helix-tasks` mounts and `AgentToolsRev` flips for an armed-with-nothing-else task; `GET task`/REST view show the pair; `mcp_backend_spectask` tools/list = prior surface ∪ pair, nothing else changed; un-armed org tasks in an Agent-owned project are unchanged.
- [ ] Guardrail test: a spec-task planning session never carries `Metadata.OrgWorkerID`, and a task key on `/mcp/helix-org` still 403s while armed tasks work on `/mcp/helix-tasks`.
- [ ] Unit tests: stamp cases (Bot/principal/dedup); `BoundAgentForProject` 1/0/2/allowlist-only/human; get_secret+list_secrets principal-bound / principal-unbound / unarmed-defense / bot-unchanged with audit-actor checks; prompt table (armed-no-delegation → hint only; delegation-only → unchanged; both → both); server surface tests above.
- [ ] Build + test: `go build ./pkg/...` for touched packages, `CGO_ENABLED=1 go test ./pkg/org/... ./pkg/services/ ./pkg/server/ -count=1`, `./stack update_openapi` if DTOs changed (expect: none).
- [ ] Inner-Helix e2e: hire Bot + grant a Helix secret; spawn a task → `GET task` shows the pair in `agent_tools`, tools/list has them with no project-allowlist entry, `list_secrets` shows the name and no value, `get_secret` resolves; UI-created task in the same bot project shows nothing until admin adds `get_secret` to `agent_tools` via REST, then it resolves same-scoped; manager Bot's managed `ProjectIDs` includes the project but grants nothing; rotate the secret value → next call returns it; fire the Bot → calls error cleanly, task still chats/approves/starts fine, spawn another task → its stamp calls fail closed cleanly.
- [ ] Regression e2e: full planning→implementation→approval→PR cycle on a plain un-armed spec task in this project — zero visible change — and confirm sub-tasks spawned by an armed task are also armed and resolve their own project's agent.
