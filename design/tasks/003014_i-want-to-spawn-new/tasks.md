# Implementation Tasks: Give Spawned Spec Tasks Their Agent's Tool Access

- [ ] Add `BoundAgentForProject(ctx, st *store.Store, orgID, projectID) (orgchart.NodeID, error)` and `AgentToolNames(ctx, st *store.Store, orgID, nodeID) ([]tool.Name, error)` to `api/pkg/org/infrastructure/runtime/helix`: `Nodes.List`→Bot filter→`LoadState` home-project match (1 → id; 0 → typed `ErrNoBoundAgent`; >1 → WARN + same error; never `Bot.ProjectIDs`); `Nodes.Get`→`Node.Tools`, nil-safe.
- [ ] In `helix/spectasks.go` `Create` (port funnel = every org spawn): append `"*"` to the new task's `AgentTools` if absent (Bot and task-principal callers alike). No new args/columns; REST/UI creation untouched. One line in the `create_spectask` description: "grants the task its agent's Helix tools while the bond exists".
- [ ] `runtime/principal.go`: add `WithBoundWorker`/`BoundWorkerFromContext` ctx value + `tool.SubjectForCaller(ctx, caller)` helper doc'd rule (Bot → `caller.ID()`; project principal → bound worker; unbound principal → typed error the tools surface as a clean message).
- [ ] Implement the surface formula once and use it at both server choke points: `server/agent_tools_handlers.go::specTaskAgentTools` and `server/mcp_backend_spectask.go::eligibleSpecTaskTools` (extract a shared helper in package server if clean): `own = EffectiveAgentTools(union minus "*") ∩ SpecTaskAgentTools; armed = raw task.AgentTools contains "*" ? AgentToolNames(BoundAgentForProject) : ∅; union deduped`. Wire org-store access to `specTaskAgentTools` via the composition root so the zed-config sites (`zed_config_handlers.go:193/538`) get mount + rev from the same list; keep `"*"` unserved (sanitizers never receive it).
- [ ] `server/mcp_backend_spectask.go`: resolve the bound agent per request (cache per request) and stash via `WithBoundWorker` so tools can resolve subjects.
- [ ] `mcptools/get_secret.go` + `list_secrets.go`: principal branch now comes free via `SubjectForCaller` (bound worker id → `workersecrets.Service`; audit `Recorder` actor stays the task id; unbound → clean error; Bot path unchanged).
- [ ] Identity audit across every tool reachable through a Bot's `Bot.Tools`: for each, apply and verify `SubjectForCaller` semantics, or make it fail loudly with zero side effects for delegated callers. Deliverable: a table in the PR marking each tool class (on-behalf-of / task-scoped / unsupported-delegation) — do not start implementation until this list exists (`mcptools/builtins.go` registry is the inventory).
- [ ] Prompts: `services.BuildAgentToolsSection` stops enumerating `"*"` (and any non-delegation names) into the delegation section; renders a distinct identity hint when raw `task.AgentTools` contains `"*"`. Verify both call sites (`spec_driven_task_service.go:409`, `agent_instruction_service.go:646`).
- [ ] Unit tests per design §Tests: stamp cases; helpers 0/1/2/allowlist-only; surface formula table (unarmed ≡ today, armed+bound = own∪live exactly, armed+unbound = own only, `"*"` never in tools/list); rev flips on simulated agent-tool-set change; tools/list ≡ rev ≡ REST view; `SubjectForCaller` cases; representatives chat-as-Agent / managers-on-behalf / one unsupported-errors-loudly-with-zero-writes; prompt gates; `metadata.OrgWorkerID` guardrail (task keys 403 on helix-org).
- [ ] Build + test: `go build ./pkg/org/... ./pkg/server/ ./pkg/services/`, `CGO_ENABLED=1 go test ./pkg/org/... ./pkg/services/ ./pkg/server/ -count=1`; openapi regen only if DTOs touched (expect none).
- [ ] Inner-Helix e2e: hire Bot (own project default tools incl `chat`+`get_secret`), grant a Helix secret; Bot spawns task → tool surface shows Bot's set + task tools, `agent_tools` row shows `["*"]`; `chat` posts to a stream as the Agent (audit actor = task id); `get_secret` reads the bound agent's value; `attach_tool` a new tool to the Bot → restart task desktop → new tool present (rev flip); `detach_tool` → gone; fire the Bot → task restart shows only own task tools with clean org-call errors, task still plans/implements/approves fine; UI-created task in same project unarmed → nothing leaks; admin adds `"*"` via REST → arms identically, remove → disarms; armed task spawns child (same project) → child armed and resolves the project agent.
