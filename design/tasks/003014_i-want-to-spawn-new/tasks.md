# Implementation Tasks: Grant Worker Secrets to Spawned Spec Tasks

- [ ] Add `SecretsWorkerID` (indexed size:255) and `SecretGrants []string` (jsonb) fields to `types.SpecTask` in `api/pkg/types/simple_spec_task.go`; confirm GORM AutoMigrate creates them; verify REST spec-task create/update handlers neither read nor persist them.
- [ ] Add `SecretGrants []string` to `runtime.CreateSpecTaskInput` (`api/pkg/org/infrastructure/runtime/runtime.go`) and forward it verbatim through `application/spectasks.Service.Create`.
- [ ] Add optional `secret_grants` arg + description text to `mcptools.createSpecTaskArgs` in `api/pkg/org/interfaces/mcptools/spec_tasks.go`; document "names come from your list_secrets; subsets only" and update the tool description.
- [ ] In `infrastructure/runtime/helix/spectasks.go` `Create`: persist provenance; validate each requested name exists on the calling Worker's bindings (read via `orgStore`'s WorkerSecretBindings repo); ProjectPrincipal (sub-task) callers inherit the parent task's `SecretsWorkerID` and are rejected unless grants are a subset of the parent's grants — resolve parent via the `ProjectPrincipal`/session-task lookup; error messages list the permitted names.
- [ ] Add `SpecTaskSecretSource func(ctx, orgID, taskID) (orgchart.NodeID, []string, error)` seam to `mcptools.Deps` (+ `Config`), wired in `api/pkg/server/helix_org.go` over the Helix store; return a "no grants" error for tasks without grants.
- [ ] Branch `get_secret.go` and `list_secrets.go` on `runtime.ProjectPrincipalFromContext(ctx)`: resolve the source Worker + allowed names via the seam, enforce the name allowlist before resolving values, keep audit `Recorder` attributing to the task caller id; Bot behaviour unchanged.
- [ ] In `api/pkg/server/mcp_backend_spectask.go` `eligibleSpecTaskTools`: append `ListSecretsName`/`GetSecretName` when `task.SecretGrants` is non-empty, independent of the project allowlist; and add both names to `mcptools.SpecTaskAgentTools` in `defaults.go` so stale AgentTools entries alone can never grant secrets.
- [ ] Add `EffectiveSpecTaskAgentTools(projectTools, taskTools []string, hasSecretGrants bool)` helper in `api/pkg/types/agent_tools.go`; use it at the three existing surfaces that compute a task's tool list so tools/list, cache-bust rev, and REST view stay in sync: `external-agent/zed_config.go` (helix-tasks `AgentToolsRev`), `services/spec_task_prompts.go` `BuildAgentToolsSection`, `server/agent_tools_handlers.go`.
- [ ] Extend the spec-task prompt so a task with grants gets a short "credentials available — use list_secrets/get_secret" hint (names + usage only).
- [ ] Unit tests: helix runtime `Create` provenance/validation/subset cases (`spectasks_test.go`); tool-branch tests in `mcptools` over a fake `SpecTaskSecretSource` mirroring `worker_secrets_test.go`; `mcp_backend_spectask` tools/list surface + rev-change tests.
- [ ] Build + verify: `go build ./pkg/server/ ./pkg/store/ ./pkg/types/ ./pkg/org/...`, `CGO_ENABLED=1 go test ./pkg/org/... ./pkg/types/`, swagger `./stack update_openapi` if any handler DTOs were touched.
- [ ] End-to-end in the inner Helix (`http://localhost:8080`): hire/start a test Bot with a granted Helix secret, `create_spectask` with `secret_grants` (and a negative with an unknown name), start the task agent, verify tools/list contains `list_secrets`/`get_secret` only on the granted task, values resolve, then ungrant on the Bot and confirm the next `get_secret` fails; check the audit recorder rows show the task as actor.
- [ ] Confirm no regression for tasks with no grants: secret tools absent from tools/list, prompt has no credential hint, no tasks with `secrets_worker_id` set via UI/REST creation.
