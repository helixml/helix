# Implementation Tasks: Grant Worker Secrets to Spawned Spec Tasks

- [ ] Add `SecretsWorkerID string` (indexed size:255) to `types.SpecTask` in `api/pkg/types/simple_spec_task.go`; confirm GORM AutoMigrate creates it; verify REST spec-task create/update handlers neither accept nor persist it and the task-clone path clears it.
- [ ] In `infrastructure/runtime/helix/spectasks.go` `Create`: Worker-session calls stamp `task.SecretsWorkerID = workerID`; ProjectPrincipal calls (sub-task spawns) load the caller's task row by `inv.Caller.ID()` and inherit the parent's `SecretsWorkerID`, failing closed (no provenance) when that lookup fails. No `create_spectask` argument changes.
- [ ] Add `SpecTaskSecretsWorker func(ctx, orgID, taskID) (orgchart.NodeID, error)` seam to `mcptools.Deps` (+ `Config`), wired in `api/pkg/server/helix_org.go` over the Helix store; error when the task has no `SecretsWorkerID`; recoverable error (no panic) when the referenced Worker row is gone.
- [ ] Branch `api/pkg/org/interfaces/mcptools/get_secret.go` and `list_secrets.go` on `runtime.ProjectPrincipalFromContext(ctx)`: for spec-task callers resolve the source Worker via the seam and read through the existing `workersecrets.Service` with that id; audit `Recorder` must still attribute each read to the task id as actor; Bot behaviour unchanged.
- [ ] In `api/pkg/server/mcp_backend_spectask.go`: append `mcptools.ListSecretsName`/`GetSecretName` to the effective set when `task.SecretsWorkerID != ""`, bypassing the project allowlist; add both names to `mcptools.SpecTaskAgentTools` in `defaults.go` so provenance-less tasks can never surface them.
- [ ] Add `EffectiveSpecTaskAgentTools(projectTools, taskTools []string, hasSecretProvenance bool)` in `api/pkg/types/agent_tools.go` (appends the pair after the existing intersect, deduped, never removes) and use it at the two tool-SURFACE call sites: `external-agent/zed_config.go` (`specTaskTools`/`AgentToolsRev` helix-tasks URL) and `server/agent_tools_handlers.go`. Do NOT feed it to the delegation enumeration in `services/spec_task_prompts.go`.
- [ ] Extend the planning prompt build: a credential hint section gated on the provenance flag ALONE (independent of `BuildAgentToolsSection`'s `len(tools)==0` gate), so a provenance-only task gets the hint and never sees the delegation section; existing delegation rendering byte-unchanged for all tool combinations.
- [ ] Unit tests: provenance stamping + inheritance + fail-closed in `helix/spectasks_test.go`; get_secret/list_secrets spec-task-caller tests over a fake seam (patterns: `worker_secrets_test.go`, `spec_tasks_test.go`) including a gone/missing worker returning a clean error; `mcp_backend_spectask` tests asserting tools/list = prior surface ∪ {pair} exactly (nothing existing removed) and rev changes both ways; `EffectiveSpecTaskAgentTools` table test incl. additivity property; prompt tests: provenance-without-delegation-tools renders credential hint but not the delegation section.
- [ ] Build + verify: `go build ./pkg/server/ ./pkg/store/ ./pkg/types/ ./pkg/org/...`, `CGO_ENABLED=1 go test ./pkg/org/... ./pkg/types/`; run `./stack update_openapi` if any swagger DTOs changed.
- [ ] End-to-end in the inner Helix (`http://localhost:8080`): hire/start a test Bot with a granted Helix secret, `create_spectask` from the Bot, start the task, verify tools/list includes `list_secrets`/`get_secret` on `helix-tasks`, `list_secrets` shows the name and no value, `get_secret` resolves it; ungrant on the Bot and confirm the task's next `get_secret` fails while a new `list_secrets` shows it gone; check audit rows show the task as actor and the Bot+secret as subject.
- [ ] Confirm no regression for provenance-less tasks: REST-created task's tools/list lacks both secret tools, prompt has no credential hint, cloned tasks carry no `secrets_worker_id`.
- [ ] Confirm standalone usability in the inner Helix: take the Worker-spawned task and run planning → implementation → approval → PR entirely through the normal UI as the human owner (no org/Bot session); then fire the spawning Bot and verify the task still starts, chats, and completes turns — only the two secret tools fail, with a clean error.
