# Implementation Tasks: Grant Worker Secrets to Spawned Spec Tasks

- [ ] Add `BoundAgentForProject(ctx, st *store.Store, orgID, projectID) (orgchart.NodeID, error)` to `api/pkg/org/infrastructure/runtime/helix` (alongside `LoadState`): `Nodes.List(orgID)` → filter Bot nodes → `LoadState` each → match `state.ProjectID == projectID` (ownership ONLY; never `Bot.ProjectIDs` allowlist). Exactly 1 → its id; 0 → typed `ErrNoBoundAgent`; >1 → WARN log + same typed error (fail closed). No cache in v1.
- [ ] Add `WithBoundWorker` / `BoundWorkerFromContext` ctx value to `api/pkg/org/infrastructure/runtime/principal.go` (sibling of `ProjectPrincipal`).
- [ ] `api/pkg/server/mcp_backend_spectask.go`: after existing project/task checks, call `BoundAgentForProject` (org store handle reachable via the org-handler wiring in `helix_org.go`); stash on ctx when resolved; in `eligibleSpecTaskTools` append `mcptools.ListSecretsName`/`GetSecretName` iff resolved, deduped — tools/list must equal the rev input.
- [ ] `api/pkg/org/interfaces/mcptools/get_secret.go` and `list_secrets.go`: on `ProjectPrincipalFromContext(ctx)` require `BoundWorkerFromContext(ctx)`; resolve via the existing `workersecrets.Service` with that worker id (keep `Recorder` attributing to the task caller id); return a clean "project not bound to a helix-org agent" error when absent. Bot path byte-identical. No new `mcptools.Deps` seam.
- [ ] `api/pkg/server/agent_tools_handlers.go` `specTaskAgentTools`: when `BoundAgentForProject` resolves for the task's project, append the pair to the returned surface (after `sanitizeAgentTools`/`EffectiveAgentTools`). This single choke point must cover both `GenerateZedMCPConfig` call sites (`zed_config_handlers.go:193/538` → mount + `AgentToolsRev`) and the REST view; verify the rev flips when a bot is hired/unhired or re-homed, applying at next desktop config refresh.
- [ ] Wire the bound-agent check as an injected func into `SpecDrivenTaskService`; at `spec_driven_task_service.go:409` and `agent_instruction_service.go:646`, when bound (`task.OrganizationID` + `task.ProjectID`), append the dedicated "inherited credentials — use list_secrets/get_secret" prompt fragment. `BuildAgentToolsSection`'s delegation enumeration and `len(tools)==0` gate must stay CRUD-only: a bound task with no delegation tools gets the hint and never the delegation section.
- [ ] Guardrail: prove spec-task sessions never receive `Metadata.OrgWorkerID` (that flag authorizes the full bot surface at `mcp_backend_helix_org.go:50`); add a test asserting a spec-task session + helix-org key still 403s there while a bound task works via helix-tasks.
- [ ] Unit tests: `BoundAgentForProject` (1/0/2/allowlist-only/human-node cases); get_secret/list_secrets principal+bound / principal+unbound / bot-unchanged with audit actor = task id; `specTaskAgentTools` pair added iff bound, including a bound task with empty tools list (helix-tasks gets mounted by the pair alone) and unbound project with stale allowlist names (pair absent); mcp_backend tools/list ≡ rev input, prior surface ∪ pair only; services hint-gate table.
- [ ] Build + test: `go build ./pkg/server/ ./pkg/types/ ./pkg/org/...`, `CGO_ENABLED=1 go test ./pkg/org/... ./pkg/services/ ./pkg/server/ -count=1`; `./stack update_openapi` only if DTOs touched (expect none — verify no schema/contract changes: no new columns, `create_spectask` schema untouched, clone path untouched).
- [ ] Inner-Helix e2e: bind a Bot (its own project) with a Helix secret; from the Bot spawn a task AND create a task in that project via UI — both must list the pair without any project allowlist entry, `list_secrets` shows the name and no value, `get_secret` resolves it; second manager Bot with `ProjectIDs` including that project must grant nothing extra; run a task in an unbound org project (this specs' own project) and a full planning→implementation→PR UI cycle with no secret tools visible anywhere.
- [ ] Lifecycle/degradation e2e: fire the bound Bot → task restart shows pair gone with clean tool errors and remains fully usable; grant a DIFFERENT bot the project home (re-hire) → next config refresh picks the new bonds; rotate the secret value → next `get_secret` returns the new one without restart.
