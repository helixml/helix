# Implementation Tasks

- [ ] In `DesignReviewContent.tsx`, add `display: { xs: 'none', sm: 'flex' }` to the git branch/commit Chip's `sx` prop (around line 795)
- [ ] In `DesignReviewContent.tsx`, add `display: { xs: 'none', sm: 'block' }` to the timestamp Typography's `sx` prop (around line 802)
- [ ] Test on mobile viewport (< 600px) to confirm tabs are visible
- [ ] Test on desktop viewport (≥ 600px) to confirm git info and timestamp still appear