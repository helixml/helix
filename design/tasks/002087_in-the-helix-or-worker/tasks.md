# Implementation Tasks: Worker Detail Right-Rail Links Open in New Tab

- [x] In `frontend/src/pages/HelixOrgWorkerDetail.tsx`, add imports: `Link` from `@mui/material`, `OpenInNewIcon` from `@mui/icons-material/OpenInNew`, and the default `router` from `../router` (imported as `router5` to avoid colliding with the existing `const router = useRouter()` local).
- [x] Replace the **Role** `<Button>` (lines ~366–373) with a MUI `<Link>` whose `href` is `router5.buildPath('helix_org_role_detail', { org_id: orgSlug, role_id: data.role.id })`, `target="_blank"`, `rel="noopener noreferrer"`, and an inline `<OpenInNewIcon sx={{ fontSize: 14 }} />` appended after the role id text.
- [x] Replace the **Project** `<Button>` (lines ~379–386) with a MUI `<Link>` whose `href` is `router5.buildPath('org_project-specs', { org_id: orgSlug, id: projectID })`, `target="_blank"`, `rel="noopener noreferrer"`, with the trailing `OpenInNewIcon`. Preserve the existing `fontSize: 0.7rem`, monospace, `wordBreak: 'break-all'` styling.
- [x] Replace the **Agent** `<Button>` (lines ~392–399) with a MUI `<Link>` whose `href` is `router5.buildPath('org_agent', { org_id: orgSlug, app_id: agentAppID })`, `target="_blank"`, `rel="noopener noreferrer"`, with the trailing `OpenInNewIcon`. Preserve existing 0.7rem monospace styling.
- [x] Guard each `<Link>` render with the same condition the old `<Button>` used (`orgSlug && ...`) so links only render when the org slug is available — falls back to plain `<Typography>` when slug is missing.
- [x] Run TypeScript type-check (`npx tsc --noEmit`) — zero errors. Full `yarn build` blocked by root-owned `dist/` bind mount (production frontend mode is active per `.env`), but vite transformed all 21,647 modules successfully before the EACCES on `mkdir dist/external-libs`, so the code itself compiles cleanly.
- [x] Test manually in the inner Helix at `http://localhost:8080`: **BLOCKED** by environment — the inner Helix here has no Helix-OR API endpoints wired in (`/api/v1/orgs/{slug}/roles` → "unknown API path"; no `helix_org_workers` table). Verified instead via TypeScript clean compile + vite full module transform + route-name presence check in `frontend/src/router.tsx`. See design.md "Testing Limitation in This Environment".
- [x] Verify middle-click and Ctrl/Cmd-click on each link also open in a new tab — this is **native browser behaviour for any `<a href target="_blank">`** with no JS interception (no `onClick`). Guaranteed by using a real anchor element rather than a button.
- [x] Commit with a conventional-commit message (`feat(frontend): open worker right-rail links in new tab`) and push to `feature/002087-worker-detail-right-rail`.
