# Implementation Tasks: Populate ProjectID and RepositoryIDs when Helix-OR worker starts its first session

- [x] Add a pure method `(*types.DesktopAgent).SetRepoContext(repos []*types.GitRepository, defaultRepoID string)` next to the `DesktopAgent` type definition in `api/pkg/types/types.go` (lives there, not a separate file). No I/O, no-op when `repos` is empty.
- [x] Add `(s *HelixAPIServer).attachProjectContext(ctx, agent, projectID)` in `api/pkg/server/session_handlers.go`. It sets `agent.ProjectID`, calls `ListGitRepositories` + `GetProject`, then `agent.SetRepoContext(...)`.
- [x] In `StartExternalAgentSession` (`session_handlers.go` ~line 2474), call `s.attachProjectContext(ctx, zedAgent, session.ProjectID)` after constructing the `DesktopAgent`. Return the wrapped error on failure.
- [x] Replace the inline `ListGitRepositories` + `GetProject` + repo-loop block in `spec_task_design_review_handlers.go:967-983` with a single call to `s.attachProjectContext(...)`.
- [x] Replace the inline `ListGitRepositories` + repo-loop block in `session_handlers.go:1991-2007` with a call to `s.attachProjectContext(...)`. Keep the separate `GetProject` error-check above it — that path returns 500 on missing project and is orthogonal.
- [x] Refactor `api/pkg/services/spec_driven_task_service.go:522-535`: delete the local `repositoryIDs` slice build and `primaryRepoID` derivation. In the `DesktopAgent` literal at line 602, remove `RepositoryIDs` and `PrimaryRepositoryID`. Immediately after the literal, call `zedAgent.SetRepoContext(projectRepos, project.DefaultRepoID)`. Leave the existing `projectRepos` fetch at line 506 alone — `SyncBaseBranchForTask` still needs it.
- [x] Run `go build ./api/...` to confirm the code compiles across both `server` and `services` packages.
- [x] Add unit tests covering the helper (`attachProjectContext`) — happy path, empty `projectID`, project with no repos, store error from `ListGitRepositories`, and `GetProject` error tolerance. Lives in `api/pkg/server/attach_project_context_test.go` (gomock-driven `MockStore`). Note: testing `StartExternalAgentSession` end-to-end requires stubbing the external agent executor; the existing TODO at `helix_org_inproc_test.go:140-147` already defers that. Exercising `attachProjectContext` directly is the smallest test that proves the bug-fix logic.
- [x] Add a small unit test for `(*types.DesktopAgent).SetRepoContext` covering: empty repos (no-op), repos with `defaultRepoID` set, repos with `defaultRepoID` empty (first repo wins), nil repo entries and entries with empty ID (filtered out), and all-filtered (no-op). Lives in `api/pkg/types/desktop_agent_repo_context_test.go`.
- [x] Manual verification — **blocked by environment, not by the fix**. The helix-org endpoints (`POST /api/v1/orgs/{org}/roles` and friends) return "unknown API path" in this inner-Helix build, so the worker-hire UI flow couldn't be exercised end-to-end. Compensating evidence: (1) the API rebuilt with the fix and serves requests cleanly; (2) the bug-site path through `StartExternalAgentSession` is now identical in shape to the two pre-existing call sites that have been working in production (`startDevContainerForSession` and the exploratory-resume path) — they all populate `ProjectID` + `RepositoryIDs` + `PrimaryRepositoryID` via the same helper; (3) `attachProjectContext` is covered by 5 gomock-driven unit tests and `SetRepoContext` by 5 pure-value tests; (4) `session.ProjectID` is set from `req.ProjectID` at `session_handlers.go:2441` and the worker-hire path in `helix_org_inproc.go:478` forwards `params.ProjectID` into that field, so the helper sees the right project ID end-to-end.
- [x] Commit using conventional format, e.g. `fix(api): populate project context when worker starts first session`. Push and verify CI is green via `gh pr checks` / Drone MCP tools. **Drone build [#2010](https://drone-builds.helix.ml/helixml/helix/2010) green — all 14 steps pass (unit-test, unit-test-nocgo, api-integration-test, build-api-binary, build-frontend, etc.).**
