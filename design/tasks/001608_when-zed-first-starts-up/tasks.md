# Implementation Tasks

- [x] In `crates/agent_ui/src/agent_panel.rs`, add `if cfg!(feature = "external_websocket_sync") { return false; }` at the top of `should_render_onboarding()` (around line 3051), with a brief comment explaining why
- [x] Open a PR against the `zed-4` repo with an imperative title and a `Release Notes:` section
