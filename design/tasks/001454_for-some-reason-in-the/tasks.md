# Implementation Tasks

- [ ] In `frontend/src/components/tasks/BacklogTableView.tsx` (lines 126–127), change `a.created` → `a.created_at` and `b.created` → `b.created_at` in the secondary sort
- [ ] In `frontend/src/components/tasks/SpecTaskKanbanBoard.tsx` (lines 817–818, backlog column sort), change `b.created` → `b.created_at` and `a.created` → `a.created_at`
- [ ] Run `cd frontend && yarn build` to verify no TypeScript errors
