# Requirements: Merge Latest Zed Upstream (002100)

## Context

Today is **2026-06-15**. The Helix fork of Zed last merged `zed-industries/zed` via **task 002077** (PR `helixml/zed#61`, merge commit `f82e1c6760`, integrating upstream `992f395c3d` "editor: Fix columnar selection alignment on rows with multi-byte chars (#57097)") on **2026-06-12**. That merge already shipped PR #60's `ede_diagnostic` retry loop into the sandbox image.

Confirmed at runtime on `/home/retro/work/zed` on 2026-06-15:

| | Commit | Date |
|---|---|---|
| Fork HEAD (`origin/main`) | `f82e1c6760` ("Merge pull request #61 from helixml/feature/002077-merge-latest-zed") | 2026-06-12 |
| Helix `sandbox-versions.txt` `ZED_COMMIT` | `f82e1c6760` (**already at fork HEAD** — no sandbox catch-up debt) | 2026-06-12 |
| Last upstream merge fence | `992f395c3d` ("editor: Fix columnar selection alignment…") — absorbed in 002077 | 2026-06-12 |
| Upstream HEAD (`upstream/main`) | `cccc7b2d44` ("editor: Fix splitting brackets inside line comments (#59260)") | 2026-06-15 |
| Helix-only commits since 002077 | **0** (fork main has not moved post-merge) | 3 days |
| Upstream commits to merge (`992f395c3d..upstream/main`) | **21** | 3 days |
| `upstream` git remote configured locally | **No** in fresh clones — must be added |

### Risk profile: LOW (smallest catch-up window of any merge in this series)

The window is tiny — 21 commits in 3 days, vs the 256-commit / 10-day 002077 catch-up. More importantly, upstream activity in this window concentrates in territory Helix does not touch:

| File | Commits | +/- lines | Risk | Concrete concern |
|---|---|---|---|---|
| `crates/acp_thread/` | **0** | **0** | NONE | No upstream churn — PR #55 emit, Critical Fixes #6/#8/#9 untouched. |
| `crates/agent/src/` | **0** | **0** | NONE | No upstream churn — Critical Fix #1 (`load_session`), `wait_for_tools_ready`, the compaction state machine untouched. |
| `crates/workspace/src/workspace.rs` | **0** | **0** | NONE | No further typed-error tightening; `CollaboratorId::Agent` follow-focus guard untouched. |
| `crates/zed/src/main.rs` | **0** | **0** | NONE | `--allow-multiple-instances`, `--headless`, `build_application(headless: bool)` untouched. |
| `crates/title_bar/` | **0** | **0** | NONE | `external_websocket_sync` optional dep + `render_restricted_mode` cfg gate untouched. |
| `crates/feature_flags/` | **0** | **0** | NONE | `AcpBetaFeatureFlag::enabled_for_all() -> true` override untouched. |
| `crates/agent_servers/src/acp.rs` | **0** | **0** | NONE | PR #50 `session_creation_chain` + `_settings_subscription` untouched. |
| `crates/agent_settings/`, `crates/settings_content/` | **0** | **0** | NONE | `show_onboarding`/`auto_open_panel`/`sandbox_permissions`/`auto_compact` coexistence not retested this window. |
| `crates/external_websocket_sync/` | **0** | **0** | NONE | PR #60 retry loop in `handle_follow_up_message` untouched (no fork commits since 002077 either). |
| `crates/agent_ui/src/agent_panel.rs` | **1** | **+1 / -5** | **LOW** | `1e017d04b9` "Remove dead link in agent menu (#59232)" deletes a `Rules Library` menu entry at upstream line 5690 — well below Fix 1b at line 5420. Expected to auto-merge cleanly. |
| `crates/extensions_ui/src/extensions_ui.rs` | **1** | **+27 / -24** | **LOW** | `f39cf25c0b` "Hide agent servers from chips (#59231)" restructures the `ExtensionProvides::iter()` chip filter at upstream line 1738. Helix `// HELIX: External agent…` bypass markers sit at lines 226, 248, 1518 — different regions. Expected to auto-merge; verify markers post-merge. |
| `crates/agent_ui/src/threads_archive_view.rs` | **1** | **+22 / -9** | LOW | `df9c9f055e` "Match project name in archive-view search (#58214)" — Helix has no patch here. |
| `crates/agent_ui/src/completion_provider.rs` | **1** | **+4 / -4** | LOW | `c7987fabf7` "Truncate long model names in config option selector (#57808)" — Helix has no patch here. |
| `crates/agent_ui/src/config_options.rs` | **1** | **+8 / -1** | LOW | Same commit as above — Helix has no patch here. |
| `Cargo.toml` | small | small | LOW | `objc2`/`objc2-app-kit` version bump + `async-process` `[patch.crates-io]` entry; no impact on Helix workspace members, `external_websocket_sync` dep, `cloud_api_types`, or `rust-embed`'s `debug-embed` feature. |
| `Cargo.lock` | always | always | TRIVIAL | `--theirs`; regenerated by the next build. |
| Everything else (`gpui_macos/`, `editor/`, `notebook_ui.rs`, `util/command/darwin.rs`, `multi_buffer.rs`, `fuzzy/`, `settings_store.rs`, `gpui_macos/window.rs` +222 lines, …) | many | ~1200 lines | NONE | All outside Helix surface. |

### Specific commits worth naming (carried-over caution from prior merges, but no overlap expected this window)

- **`f39cf25c0b` "extension_ui: Hide agent servers from chips (#59231)"** — restructures the chip filter (`.filter_map` → `.filter().map()`). Verify the three Helix `// HELIX: External agent…` bypass markers in `extensions_ui.rs` (currently at lines 226, 248, 1518) survive in the post-merge file. The conflict potential is structural rearrangement, not deletion.
- **`1e017d04b9` "agent_ui: Remove dead link in agent menu (#59232)"** — single hunk at the `menu.entry("Rules Library", …)` site near line 5690 in `agent_panel.rs`. **Verify Fix 1b at line 5420 still survives as the FIRST statement of `BaseView::Uninitialized`** — Phase 17 is the runtime gate. Position regression is unlikely (different region) but the file is the standard re-grep target.
- **`d4cc8d2409` "Patch async-process to allow reusing their reaper (#59156)"** — adds a `[patch.crates-io]` entry. May cause a "both sides added a patch" auto-merge in `Cargo.toml`; Helix's `Cargo.toml` does not currently carry any `[patch.crates-io]` entry, so this should land cleanly.

## User Stories

### 1. Platform Engineer (performing the merge)
> As a platform engineer, I want to absorb the 21-commit / 3-day upstream catch-up while it is still trivially small, rather than letting it grow into another 256-commit weekend.

### 2. Helix User
> As a Helix user, I want upstream bug fixes (bracket-splitting in line comments, editor multi-byte alignment, file-finder list-item design, agent-config model-name truncation, prettier `.editorconfig` support, audio phantom-presence fix, repl kernel error handling, GPUI macOS traffic-light hitbox) without losing WebSocket sync, draft suppression, headless mode, the PR #60 `claude-agent-acp` drain-race retry, or any of the 10 active Critical Fixes.

### 3. Future Merge Engineer
> As an engineer performing the next upstream merge, I want `portingguide.md` updated **as work proceeds** with a new dated entry for 002100 — even if the entry is short and reads as "21 upstream commits absorbed, zero Helix-surface conflicts, zero breakage repairs" — because the porting guide's value is its uninterrupted timeline.

## Acceptance Criteria

### Merge Completeness
- [ ] Fork main includes all upstream commits through current upstream HEAD (`cccc7b2d44` or newer; any skipped commits explicitly justified in `portingguide.md`)
- [ ] No upstream commits silently cherry-picked out
- [ ] **PR #60's retry loop in `handle_follow_up_message` is preserved verbatim** (no upstream touches `thread_service.rs` this window; a careless cleanup is the only risk)

### Critical Fix Preservation (the 10 active fixes in `portingguide.md` §"Critical Fixes" — #10 stays retired)
- [ ] Fix #1: `NativeAgent` entity cloned/`pending_sessions` shared-task pattern in `load_session()`
- [ ] Fix #2: No duplicate WebSocket sends from `crates/agent_ui/src/acp/thread_view.rs`
- [ ] Fix #3: `content_only()` strips `## Assistant` heading
- [ ] Fix #4: `notify_thread_display()` called for follow-ups to non-visible threads
- [ ] Fix #5: stale pending entries flushed when a different entry starts streaming
- [ ] Fix #6: every `send()` emits exactly one `Stopped` (`stopped_emitted_for_task` guard)
- [ ] Fix #7: `THREAD_REGISTRY` unregistration on entity replacement
- [ ] Fix #8: `cancel()` drops `send_task` instead of awaiting it
- [ ] Fix #9: `stopped_emitted_for_task` guard on the normal completion path
- [ ] Fix #11: entity-identity guard at top of `load_agent_thread` in `agent_panel.rs` (`ThreadMetadataStore` / session_id-based form from 002077)

### Helix-Specific Surface (must survive verbatim)
- [ ] `crates/external_websocket_sync/` crate intact (all source files)
- [ ] **PR #60 `handle_follow_up_message` 4×750ms retry on `ede_diagnostic` transient intact in `thread_service.rs`**
- [ ] WebSocket thread display callback + UI state query callback in `agent_panel.rs::new()`
- [ ] `acp_history_store()` accessor on `AgentPanel`
- [ ] `from_existing_thread()` constructor on `ConversationView` — re-verify field set matches `ConversationView::new` (15 fields as of 002077; no upstream churn in `conversation_view.rs` this window, so a fifth signature-drift repair is unlikely)
- [ ] PR #50 `session_creation_chain` field on `AcpConnection` + drop-guard intact, coexisting with `_settings_subscription`
- [ ] PR #50 `test_concurrent_session_creation_is_serialized` test passes
- [ ] PR #55 `EntryUpdated` emit after streaming-reveal drain in `acp_thread.rs` preserved (no upstream churn)
- [ ] PR #56 Fix 1a deferred `UserCreatedThread` plumbing in `external_websocket_sync` intact
- [ ] PR #56 Fix 1b `#[cfg(feature = "external_websocket_sync")] { return; }` still the FIRST statement of `BaseView::Uninitialized` branch in `ensure_thread_initialized` — re-verify position even though `1e017d04b9` only touches the menu region
- [ ] PR #57 Phase 16 counter exclusion in `helix-ws-test-server/main.go` intact
- [ ] `fd26c1a113` `Dockerfile.ci` `helix-org` pull intact
- [ ] `AcpBetaFeatureFlag::enabled_for_all() -> true` override intact
- [ ] Built-in agent hiding (Claude Code, Codex, Gemini hidden in Helix builds)
- [ ] Enterprise TLS skip (`sync_settings`)
- [ ] `--allow-multiple-instances` CLI flag in `crates/zed/src/main.rs`
- [ ] `--headless` CLI flag + `initialize_headless()` + `build_application(headless: bool)` pattern intact
- [ ] `rust-embed` workspace dep still has `debug-embed` feature
- [ ] `wait_for_tools_ready` uses `cx.background_executor().timer()` (not `smol::Timer`)
- [ ] `title_bar` feature propagation chain intact (`optional = true`)
- [ ] `title_bar::render_restricted_mode` cfg-gated early return intact
- [ ] `extensions_ui.rs` `// HELIX: External agent …` bypass markers retained at lines 226, 248, 1518 (line numbers may shift after `f39cf25c0b` restructures the chip filter — confirm by `grep -n "HELIX: External agent"`)
- [ ] `BaseView` / `ContextServerStatus` exhaustive matches in Helix-mode code updated for any new variants (no upstream churn in `acp_thread.rs` / `agent_panel.rs` core state — unlikely)

### Trait/API Migrations Forced by Upstream
- [ ] No new typed-error migration sites surfaced (`Workspace::show_error` untouched this window)
- [ ] No new `ToolCall::status` rewrites (no `acp_thread.rs` churn)
- [ ] No new compaction-cluster fields requiring three-way coexistence (no `agent_settings`/`settings_content` churn)
- [ ] No new flush-on-quit interaction risk (no `agent/src/agent.rs` churn)
- [ ] No new draft-inheritance path that bypasses Fix 1b (only the menu-link removal touches `agent_panel.rs`)

### Build & Test (hard gates)
- [ ] `cd /home/retro/work/helix && ./stack build-zed dev` succeeds — zero errors
- [ ] (If local Rust toolchain available) `cargo check -p zed --features external_websocket_sync` — zero errors
- [ ] (If local Rust toolchain available) `cargo test -p external_websocket_sync` — full pass (≤2 env-dependent ignored acceptable)
- [ ] (If local Rust toolchain available) `cargo test -p acp_thread test_second_send` — passes (Fix #6 invariant)
- [ ] (If local Rust toolchain available) `cargo test -p agent_servers test_concurrent_session_creation_is_serialized` — passes
- [ ] **External WebSocket sync E2E (Docker, hard gate)** — all 17 phases pass for **both** `zed-agent` and `claude` agents. Contractual minimum:
  - Phases 1–7 (basic sync + non-visible follow-up + UI state + open_thread)
  - Phase 8 (mid-stream interrupt — Stopped invariant)
  - **Phase 9 (rapid 3-turn cancel — PR #60 retry-loop regression gate)**
  - Phase 10 (user_created_thread)
  - Phase 11 (spectask routing)
  - Phase 12 (reconnect)
  - Phase 13 (`cancel_current_turn` happy path — `turn_cancelled` ordering)
  - Phase 14 (`cancel_current_turn` no-op)
  - **Phase 15** (streaming patches arrive incrementally — PR #55 emit gate)
  - **Phase 16** (zero spontaneous `UserCreatedThread` emits — PR #56 Fix 1a + PR #57)
  - **Phase 17** (live Claude process count == real thread count — PR #56 Fix 1b draft-suppression gate)
- [ ] Claude E2E Phase 1 npm-install bootstrap flake (lesson from 001996): one retry permitted

### Documentation (hard gate — written incrementally)
- [ ] `portingguide.md` updated **as each conflict is resolved**, not at the end (even if the merge is conflict-free, the entry is still written incrementally — start it when the merge command is issued)
- [ ] New `## Merge 002100 (2026-06-15)` section appended at the top of the merge-history list
- [ ] Per-conflict subsection in `### Conflicts and Resolutions` (or an explicit "0 conflicts, auto-merge clean" note if that holds)
- [ ] **Helix-surface survival check** subsection — explicit per-area confirmation that no manual repair was needed (`agent_panel.rs` Fix 1b position; `extensions_ui.rs` three bypass markers; PR #50/#55/#56/#60 intact)
- [ ] `### Pre-existing Breakage Repaired` subsection only if any signature-drift / typed-error / new-variant fix actually fires (otherwise omit — do not invent updates)
- [ ] Commit-history table at the bottom of `portingguide.md` extended with this merge's commits
- [ ] Stale guide entries discovered along the way are corrected or deleted

### Process
- [ ] Feature branch `feature/002100-merge-latest-zed` created from current fork main (`f82e1c6760`, or newer if anyone pushed meanwhile)
- [ ] Branch pushed to `helixml/zed`
- [ ] Helix repo `ZED_COMMIT` bump prepared in `sandbox-versions.txt` (from current `f82e1c6760` to the new merge HEAD)
- [ ] Helix branch `feature/002100-merge-latest-zed` pushed
- [ ] `pull_request_zed.md` and `pull_request_helix.md` written into this task directory
- [ ] `main` is **not** force-pushed without explicit user approval
- [ ] The Helix UI handles PR creation — do not open PRs from the agent (per task convention)

## Out of Scope

- Net-new Helix feature development
- Modifying E2E test assertions themselves (unless a legitimate upstream API change strictly requires it)
- Upstreaming Helix patches back to `zed-industries/zed`
- Refactors of Helix-specific crates beyond what conflict resolution requires
- Wiring upstream's `/compact` / `auto_compact` (002077-era) into Helix-mode UX
- Adopting upstream's typed-error infrastructure beyond what the build forces
