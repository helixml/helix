# Implementation Tasks

- [~] In `UsagePulseChart.tsx`: reduce container and chart height from 50px to 30px
- [~] In `UsagePulseChart.tsx`: remove outer `mt`/`mb` margins, reduce chart internal margins to `{ top: 2, bottom: 2 }`
- [ ] In `TaskCard.tsx` `TaskProgressDisplay`: reduce wrapper `mt` from 1.5 to 1, progress header `py` from 0.75 to 0.5
- [ ] In `TaskCard.tsx` `TaskProgressDisplay`: reduce task list container `py` from 0.5 to 0.25, per-item `py` from 0.5 to 0.25, `gap` from 0.75 to 0.5
- [ ] In `TaskCard.tsx` `TaskProgressDisplay`: reduce status icon size from 16/14px to 14/12px, spinner from 14 to 12
- [ ] In `TaskCard.tsx`: reduce label chips `mb` from 1 to 0.5
- [ ] Verify `yarn build` passes with no errors
- [ ] Visual check: confirm cards are noticeably more compact while remaining readable
