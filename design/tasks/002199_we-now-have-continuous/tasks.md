# Implementation Tasks: Reconcile Stale Dev Container State After Sandbox Restart

- [x] ~~Add a store query to list sessions for a given sandbox marked running~~ — NOT needed: `store.ListSessionsBySandbox(ctx, sandboxID)` already exists and returns all sessions on a sandbox (used by `OnSandboxDisconnected`). Reuse it and filter in-memory.
- [x] In `DiscoverContainersFromSandbox` (`api/pkg/external-agent/hydra_executor.go:1415`), build the set of session IDs hydra reports as live.
- [x] Add the "subtract" reconcile (`markMissingSessionsStopped`): for DB-running sessions on that sandbox NOT in hydra's live set, authoritatively confirm the container is gone (per-session `GetDevContainer` probe under the creation lock, to avoid racing `StartDesktop`), then set `ExternalAgentStatus = "stopped"`, clear container metadata, and evict the stale `h.sessions` entry.
- [x] Ensure the subtract pass also runs when hydra reports zero containers (placed before the `len(containers) == 0` early-return).
- [x] (Optional) Kanban live-check in `listTasks`: DECIDED NOT to add a per-task RevDial probe. The primary reconcile both sets the DB session to `stopped` AND evicts the in-memory `h.sessions` entry on reconnect, so the existing cheap `GetSession` check already returns an error → `absent` afterwards. A per-task RevDial probe on the ~3.1s poll would add a hydra round-trip per running task per poll (heavy hot-path cost) to cover only the brief pre-reconnect window, which is already handled by `OnSandboxDisconnected` on definitive disconnect. Relying on the reconnect reconciler (see design.md D2 fallback).
- [x] Add a Go unit test (gomock store) covering: hydra reports a subset → missing session becomes `stopped` with cleared metadata + in-memory eviction, live session untouched, `starting` skipped; hydra reports zero → all become `stopped`; `local`/empty sandbox skipped. (`reconcile_missing_sessions_test.go`, passes.)
- [x] Build check: `go build ./pkg/external-agent/ ./pkg/server/ ./pkg/store/ ./pkg/types/` — OK.
- [ ] E2E in inner Helix: create spec task → live container shows `running` → simulate sandbox restart (kill container / restart hydra) → force reconnect → confirm Kanban card flips to `absent` and desktop viewer no longer errors; then confirm the task can be re-started cleanly.
- [ ] Verify no false downgrade for a re-adopted container after restart.
- [ ] Redeploy hydra into the sandbox container if hydra code changed (per CLAUDE.md hot-rebuild loop) and confirm reconnect logs.
