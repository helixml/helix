# Implementation Tasks: Merge Latest Zed Upstream (002251)

## Setup & Read (mandatory before any merge resolution)

- [ ] Read `/home/retro/work/zed/portingguide.md` in full (1109 lines; Critical Fixes §222, Rebase Checklist §488, Commit History §583, latest entry `## Merge 002100-extension (2026-06-18)` §670)
- [ ] Read prior plan `002100_merge-latest-zed/` end-to-end (requirements.md, design.md, tasks.md, pull_request_zed.md, pull_request_helix.md) — closest precedent
- [ ] Skim `002077_`, `002029_`, `001996_`, `001980_merge-latest-zed/` for critical-fix preservation patterns and the recurring signature-drift sites
- [ ] Add `upstream` remote if missing: `git remote add upstream https://github.com/zed-industries/zed.git`
- [ ] `git fetch upstream --no-tags && git fetch origin`
- [ ] Re-confirm divergence at runtime: fork HEAD (`367ba0d489` at planning), upstream HEAD (`aeeacf5439` at planning; likely advanced), commit count `e45e42af6e..upstream/main` (409 at planning)
- [ ] Confirm the 3 post-002100 Helix PRs (#65 crash-frame, #66 002228 unify + prompt-queue e2e phases, #67 Codex-ACP) are on fork main and will ride the merge
- [ ] `git checkout main && git pull origin main`
- [ ] Create feature branch `feature/002251-merge-latest-zed` from fork main

## Merge Execution

- [ ] `git merge upstream/main` — expect a large conflict set (largest window in the series)
- [ ] `Cargo.lock` → `git checkout --theirs` (regenerated by build)
- [ ] `.github/workflows/` → accept upstream
- [ ] Resolve each source conflict preserving Helix behaviour; write a porting-guide bullet **as each is resolved**
- [ ] Reconcile `grep_tool.rs` 001410 `truncate_long_lines` with upstream churn (conflicted in 002100-extension)
- [ ] Reconcile `settings_content/` + `agent_settings/` new fields (both-sides-added-field pattern)
- [ ] Confirm no cfg-gated Helix dependency on the deleted `agent_configuration/{add_llm_provider_modal,configure_context_server_tools_modal}.rs`
- [ ] No conflict markers remain; commit the merge

## ACP `0.14.0 → 1.1.0` Bump (primary work item)

- [ ] Take upstream `agent-client-protocol = "=1.1.0"` pin in `Cargo.toml`
- [ ] Build-drive every broken call site; fix compile errors area by area (`agent_servers/src/acp.rs`, `acp_thread/`, `agent/src/`, and `external_websocket_sync/` if it names ACP types)
- [ ] Update all exhaustive matches for any new/renamed ACP event, stop-reason, or thread-status variant
- [ ] Reconcile tool-call content extraction with `2df74932bc` embedded-resources change in `thread_service.rs`
- [ ] Re-verify (or re-implement) Fix #6/#9 `stopped_emitted_for_task` against `e783b2f063` "defer status until turn completion" + `c3a4bda331` "emit thread status change events"
- [ ] Document every repaired site in a `### ACP 1.1.0 bump — repaired sites` porting-guide subsection
- [ ] **Escalate to the user** if any ACP-`1.1.0` change is not a mechanical fix (unclear lifecycle/semantics)

## Verify Critical Fixes (grep + build + test)

- [ ] Fix #1 `load_session`/`pending_sessions` shared-task — `agent/src/agent.rs`
- [ ] Fix #2 no duplicate WS sends — `agent_ui/src/acp/thread_view.rs`
- [ ] Fix #3 `content_only()` — `acp_thread.rs`
- [ ] Fix #4 `notify_thread_display()` — `thread_service.rs`
- [ ] Fix #5 flush-stale-pending — `thread_service.rs`
- [ ] Fix #6/#9 `stopped_emitted_for_task` guards — `acp_thread.rs` (special attention this window)
- [ ] Fix #7 `THREAD_REGISTRY` unregistration — `thread_view.rs`
- [ ] Fix #8 `drop(send_task)` — `acp_thread.rs`
- [ ] Fix #11 entity-identity guard (`ThreadMetadataStore`) — `agent_panel.rs`

## Verify Helix Surface

- [ ] `crates/external_websocket_sync/` intact incl. PR #65 crash-terminal-frame + PR #67 Codex-ACP routing
- [ ] PR #60 `ede_diagnostic` retry loop + PR #63 wedge-recovery + PR #64 `agent_ready` re-emit intact
- [ ] `send_agent_ready` / `wait_for_websocket_connected` / UI-state-query callback / `acp_history_store()` — `agent_panel.rs`
- [ ] Fix 1a deferred `UserCreatedThread`; Fix 1b draft-suppression `return;` re-located as FIRST statement of its `BaseView::Uninitialized` branch (agent_panel.rs had 20 upstream commits incl. slash-commands refactor)
- [ ] `from_existing_thread()` field set matches `ConversationView::new` (13 upstream commits; recurring signature-drift site)
- [ ] `AcpBetaFeatureFlag::enabled_for_all() -> true`
- [ ] Built-in agent hiding reconciled with `54bf918329` (default-on-install) + `1fd93cbd34` (unship shared threads)
- [ ] Enterprise TLS skip (`sync_settings`)
- [ ] `--allow-multiple-instances` + `--headless` + `build_application(headless)` — `main.rs`
- [ ] `rust-embed` `debug-embed` feature; `wait_for_tools_ready` uses `background_executor().timer()`
- [ ] `title_bar` `optional = true` + `render_restricted_mode` cfg gate
- [ ] 3× `// HELIX: External agent` bypass markers in `extensions_ui.rs`

## Build & Test (hard gates)

- [ ] `cd /home/retro/work/helix && ./stack build-zed dev` — zero errors
- [ ] (if local Rust) `cargo check -p zed --features external_websocket_sync`
- [ ] (if local Rust) `cargo test -p external_websocket_sync`
- [ ] (if local Rust) `cargo test -p acp_thread test_second_send` (Fix #6 invariant)
- [ ] (if local Rust) `cargo test -p agent_servers test_concurrent_session_creation_is_serialized`
- [ ] `go mod tidy` in `e2e-test/helix-ws-test-server/`; copy fresh binary to `e2e-test/zed-binary`
- [ ] Enumerate the current E2E phase list (grew beyond 17 with PR #66 prompt-queue phases) — do not assume a count
- [ ] E2E `zed-agent` — all phases green
- [ ] E2E `zed-agent,claude` — all phases green for both, incl. PR #66 busy-defer + interrupt phases; named gates: mid-stream interrupt / Stopped, rapid 3-turn cancel (PR #60), cancel happy-path, streaming-incremental (PR #55), zero-spontaneous-UserCreatedThread (PR #56 Fix 1a + #57), live-process==thread (PR #56 Fix 1b)
- [ ] One retry permitted per agent for the Claude Phase 1 npm-install / single-phase latency flake

## Update `portingguide.md`

- [ ] New `## Merge 002251 (2026-07-13)` section at top of merge-history list
- [ ] Window summary (409 commits / ~25 days / largest window / ACP `0.14 → 1.1` bump)
- [ ] `### ACP 1.1.0 bump — repaired sites` subsection
- [ ] `### Conflicts and Resolutions` subsection per manual conflict
- [ ] `### Retired Helix patches` (with justification) or explicit "none"
- [ ] Helix-surface survival-check subsection
- [ ] Commit-history table extended
- [ ] New Rebase-Checklist entry only if a genuinely new recurring gotcha is confirmed
- [ ] Correct/delete any stale guide entries found

## Finalise

- [ ] Re-fetch `upstream/main` + `origin/main`; if upstream advanced materially, target newer HEAD or run a `002251-extension` round on the same branch; merge any out-of-band fork-main commits first
- [ ] Push Zed branch `feature/002251-merge-latest-zed` to `origin`
- [ ] In `/home/retro/work/helix/`, bump `sandbox-versions.txt` `ZED_COMMIT` from `548da160ce…` to the new merge HEAD on branch `feature/002251-merge-latest-zed`; push
- [ ] Write `pull_request_zed.md` + `pull_request_helix.md` into this task directory
- [ ] No force-push to `main` without explicit user approval
- [ ] No agent-initiated PRs (Helix UI handles creation)
