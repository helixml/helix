# Implementation Tasks: Make Comment Resolve Button Discoverable on Narrow Screens

- [x] In `DesignReviewContent.tsx`, fix the narrow/wide decision so the stacked layout engages before the side-positioned bubble overflows (no horizontal scroll). **Changed approach during impl:** instead of a fixed window media-query breakpoint, measure the actual document-area width with a `ResizeObserver` (threshold ~1460px). Window-based `useMediaQuery` was fundamentally wrong because this component renders both standalone and embedded (workspace `TabsView`) with different chrome widths. Removed now-unused `useMediaQuery`/`useTheme`.
- [x] In `InlineCommentBubble.tsx`, replace the bare resolve `IconButton` (CheckCircleIcon) in the comment header with a `Tooltip`-wrapped labeled `Button` ("Resolve", `size="small"`, `color="success"`, `startIcon={<CheckCircleIcon fontSize="small" />}`), keeping the `onResolve(comment.id!)` call.
- [x] Add `Tooltip` to the `@mui/material` import in `InlineCommentBubble.tsx` (also removed now-unused `IconButton`).
- [x] In `CommentLogSidebar.tsx`, apply the same labeled `Button` for the resolve action (only when `!comment.resolved`), keeping the `onResolveComment(comment.id!)` call.
- [x] Add `Button` and `Tooltip` to the `@mui/material` import in `CommentLogSidebar.tsx` (removed now-unused `IconButton`).
- [x] Verify the comment header layout is intact on wide viewports (Chip left, Resolve right, no overflow). Confirmed at 1600px: side panel fully visible, Resolve button right edge within viewport.
- [x] Build the frontend (vite build succeeds; tsc --noEmit clean. Note: in-repo `dist` is a root-owned prod bind-mount — built to temp dir to verify) (`cd frontend && yarn build`).
- [x] Test end-to-end in inner Helix: verified live (seeded a review + inline comment). Medium width (1300px): stacked layout, no horizontal scroll, labeled "Resolve" clearly visible (screenshot 02). Wide (1600px): side panel, no overflow, Resolve within viewport (screenshot 03). Clicked Resolve → POST .../resolve returned 200, bubble removed, unresolved count cleared (screenshot 04).
