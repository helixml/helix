// Firefox configuration for Helix Ubuntu container
// IMPORTANT: First line must be a comment

// Node.js/Vite binds only to IPv6, so Firefox must prefer IPv6 for localhost
defaultPref("network.dns.preferIPv6", true);
