apiVersion: v1
kind: Service
metadata:
  name: {{ include "helix-runner.fullname" . }}
  labels:
    {{- include "helix-runner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helix-runner.selectorLabels" . | nindent 4 }}
