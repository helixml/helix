apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helix-runner.fullname" . }}-test-connection"
  labels:
    {{- include "helix-runner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helix-runner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
