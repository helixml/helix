-- Rename table 'sessions' to 'session'
ALTER TABLE sessions RENAME TO session;

-- Rename table 'user_meta' to 'usermeta'
ALTER TABLE user_meta RENAME TO usermeta;

-- Rename table 'api_keys' to 'api_key'
ALTER TABLE api_keys RENAME TO api_key;

