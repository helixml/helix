kind: pipeline
type: docker
name: default

trigger:
  event:
    - push
    - tag

workspace:
  base: /go
  path: src/github.com/helix-ml/helix

steps:
  - name: build-backend
    image: golang:1.25-alpine
    volumes:
      - name: go-build-cache
        path: /root/.cache/go-build
      - name: go-mod-cache
        path: /go/pkg/mod
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    commands:
      - apk add --no-cache gcc g++ musl-dev
      - CGO_ENABLED=1 go build -tags "!rocm"
    when:
      event:
        - tag
    depends_on: []

  - name: build-frontend
    image: node:23-alpine
    volumes:
      - name: yarn-cache
        path: /usr/local/share/.cache/yarn
      - name: node-modules-cache
        path: /go/src/github.com/helix-ml/helix/frontend/node_modules
    commands:
      - cd frontend
      - yarn install --frozen-lockfile --check-files
      - npx tsc --noEmit -p tsconfig.json
      - yarn test
      - yarn build
    depends_on: []

  #  - name: run-linter
  #    image: golangci/golangci-lint:v2.8.0-alpine
  #    volumes:
  #      - name: go-build-cache
  #        path: /root/.cache/go-build
  #      - name: go-mod-cache
  #        path: /go/pkg/mod
  #    commands:
  #      - golangci-lint cache clean
  #      - golangci-lint run
  #    when:
  #      event:
  #        - push
  #    depends_on: []

  - name: helm-lint
    image: alpine/helm:3.16.4
    commands:
      # Render Chart.yaml from Chart.yaml.tmpl. No DRONE_TAG on push events, so
      # the script stamps a sentinel version — fine for lint/template since the
      # rendered output is never published from this pipeline.
      - sh scripts/render-charts.sh
      # Lint both charts with default values. (helix-runner was deleted in
      # 6d660a6d1 — the sandbox absorbed the runner role. Bitnami
      # subcharts were dropped in 32133ee6d, so no `helm dependency
      # build` step is needed any more.)
      - helm lint charts/helix-controlplane
      - helm lint charts/helix-sandbox
      # Lint controlplane with production-like example values
      - helm lint charts/helix-controlplane -f charts/helix-controlplane/values-example.yaml
      # Template render to catch runtime errors (missing helpers, bad references)
      - helm template test-release charts/helix-controlplane -f charts/helix-controlplane/values-example.yaml > /dev/null
      - helm template test-release charts/helix-sandbox > /dev/null
      # Template with each sandbox values variant
      - helm template test-release charts/helix-sandbox -f charts/helix-sandbox/values-bare-metal.yaml > /dev/null
      - helm template test-release charts/helix-sandbox -f charts/helix-sandbox/values-eks.yaml > /dev/null
      - helm template test-release charts/helix-sandbox -f charts/helix-sandbox/values-gke.yaml > /dev/null
      - helm template test-release charts/helix-sandbox -f charts/helix-sandbox/values-aks.yaml > /dev/null
      # Re-render with a synthetic real-looking tag and re-lint to catch
      # template breakage that only surfaces with a non-sentinel version
      # (the tag-build pipeline runs render-charts.sh with DRONE_TAG and we
      # want to fail in PR rather than mid-release if substitution breaks).
      - DRONE_TAG=v9.9.9-rendertest sh scripts/render-charts.sh
      - helm lint charts/helix-controlplane
      - helm lint charts/helix-sandbox
    when:
      event:
        - push
    depends_on: []

  - name: check-for-secrets
    image: ghcr.io/gitleaks/gitleaks:v8.24.0
    commands:
      - gitleaks dir -v; rc=$?; if [ $rc -eq 1 ]; then echo "LEAKS FOUND"; exit 1; fi
    when:
      event:
        - push
    depends_on: [helm-lint]